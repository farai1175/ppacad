You are a senior Front-End Developer and UI/UX Designer specializing in EdTech platforms. Use this template single-page HTML html lesson page to create other similarly structured, formated and styled lesson web pages given lesson text. Here is the template HTML/CSS/JavaScript code "<!DOCTYPE html>

<html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>Grade 12 Maths | Infinite Geometric Series | PassProof Academy</title> <link rel="preconnect" href="https://fonts.googleapis.com"> <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet"><style>
    /* ========================================
       CSS VARIABLES - Theme Configuration
       ======================================== */
    :root {
        --navy-dark: #0a1628;
        --navy: #1a2744;
        --navy-light: #2a3a5a;
        --teal: #00d4aa;
        --teal-light: #00f5c4;
        --teal-dark: #00b894;
        --white: #ffffff;
        --gray-50: #f8fafc;
        --gray-100: #f1f5f9;
        --gray-200: #e2e8f0;
        --gray-300: #cbd5e1;
        --gray-400: #94a3b8;
        --gray-500: #64748b;
        --gray-600: #475569;
        --gray-700: #334155;
        --success: #10b981;
        --success-light: #d1fae5;
        --error: #ef4444;
        --error-light: #fee2e2;
        --warning: #f59e0b;
        --warning-light: #fef3c7;
        --shadow-sm: 0 2px 8px rgba(10, 22, 40, 0.08);
        --shadow-md: 0 4px 16px rgba(10, 22, 40, 0.12);
        --shadow-lg: 0 8px 32px rgba(10, 22, 40, 0.16);
        --shadow-neu: 8px 8px 16px rgba(10, 22, 40, 0.1), -8px -8px 16px rgba(255, 255, 255, 0.9);
        --radius-sm: 8px;
        --radius-md: 12px;
        --radius-lg: 16px;
        --radius-xl: 24px;
        --transition-fast: 0.15s ease;
        --transition-normal: 0.3s ease;
    }

    /* ========================================
       BASE STYLES & RESET
       ======================================== */
    *, *::before, *::after {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    html {
        scroll-behavior: smooth;
        scroll-padding-top: 140px;
    }

    body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        background: var(--gray-100);
        color: var(--gray-700);
        line-height: 1.6;
        overflow-x: hidden;
    }

    /* ========================================
       HEADER STYLES
       ======================================== */
    header {
        position: sticky;
        top: 0;
        z-index: 1000;
        background: var(--white);
        box-shadow: var(--shadow-md);
        padding: 1rem 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .logo {
        display: flex;
        align-items: center;
        gap: 12px;
        text-decoration: none;
    }

    .logo img {
        height: 45px;
        width: auto;
        border-radius: var(--radius-sm);
        transition: transform var(--transition-fast);
    }

    .logo:hover img {
        transform: scale(1.05);
    }

    header nav {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    header nav span {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-weight: 700;
        color: var(--navy);
    }

    header nav img {
        height: 28px;
        width: auto;
    }

    /* ========================================
       STICKY LESSON MAP NAVIGATION
       ======================================== */
    .sticky-nav {
        position: fixed;
        top: 70px;
        left: 0;
        right: 0;
        background: var(--navy);
        z-index: 999;
        padding: 0.75rem 1rem;
        box-shadow: var(--shadow-md);
    }

    .lesson-map {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        justify-content: center;
        list-style: none;
        max-width: 1200px;
        margin: 0 auto;
    }

    .lesson-map li {
        flex-shrink: 0;
    }

    .nav-link {
        display: inline-block;
        padding: 0.5rem 1rem;
        color: var(--gray-300);
        text-decoration: none;
        font-size: 0.85rem;
        font-weight: 500;
        border-radius: var(--radius-md);
        transition: all var(--transition-fast);
        white-space: nowrap;
    }

    .nav-link:hover, .nav-link.active {
        color: var(--white);
        background: var(--navy-light);
    }

    /* ========================================
       MAIN CONTENT AREA
       ======================================== */
    main {
        margin-top: 130px;
        padding: 1.5rem;
        max-width: 1000px;
        margin-left: auto;
        margin-right: auto;
        padding-bottom: 100px;
    }

    section {
        background: var(--white);
        border-radius: var(--radius-lg);
        box-shadow: var(--shadow-neu);
        margin-bottom: 1.5rem;
        overflow: hidden;
    }

    /* ========================================
       COLLAPSIBLE SECTION HEADERS
       ======================================== */
    .section-header {
        background: linear-gradient(135deg, var(--navy), var(--navy-light));
        padding: 1rem 1.5rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        cursor: pointer;
        user-select: none;
        transition: background var(--transition-fast);
    }

    .section-header:hover {
        background: linear-gradient(135deg, var(--navy-light), var(--navy));
    }

    .section-header h2 {
        color: var(--white);
        font-size: 1.15rem;
        font-weight: 700;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .toggle-icon {
        color: var(--teal);
        font-size: 1rem;
        transition: transform var(--transition-normal);
    }

    .section-header.collapsed .toggle-icon {
        transform: rotate(-90deg);
    }

    .section-content {
        padding: 1.5rem;
        max-height: 8000px;
        overflow: hidden;
        transition: max-height 0.5s ease, padding 0.3s ease, opacity 0.3s ease;
        opacity: 1;
    }

    .section-content.collapsed {
        max-height: 0;
        padding-top: 0;
        padding-bottom: 0;
        opacity: 0;
    }

    /* ========================================
       OBJECTIVES SECTION
       ======================================== */
    #objectives ul {
        list-style: none;
        margin-top: 1rem;
    }

    #objectives li {
        padding: 0.75rem 1rem;
        padding-left: 2.5rem;
        position: relative;
        background: var(--gray-50);
        border-radius: var(--radius-md);
        margin-bottom: 0.5rem;
        color: var(--gray-700);
        border-left: 4px solid var(--teal);
    }

    #objectives li::before {
        content: '?';
        position: absolute;
        left: 1rem;
        color: var(--teal);
        font-weight: bold;
    }

    /* ========================================
       PRE-KNOWLEDGE & BASELINE SECTIONS
       ======================================== */
    .knowledge-list, .baseline-list {
        list-style: none;
        margin-top: 1rem;
    }

    .knowledge-list li, .baseline-list li {
        padding: 0.75rem 1rem;
        background: var(--gray-50);
        border-radius: var(--radius-md);
        margin-bottom: 0.5rem;
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
    }

    .knowledge-list li::before {
        content: '?';
    }

    .baseline-list li::before {
        content: '?';
    }

    /* ========================================
       THEORY SECTION STYLES
       ======================================== */
    .theory-content h3 {
        color: var(--navy);
        font-size: 1.25rem;
        margin: 1.5rem 0 1rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding-bottom: 0.5rem;
        border-bottom: 2px solid var(--gray-200);
    }

    .theory-content h3:first-child {
        margin-top: 0;
    }

    .theory-content p {
        margin-bottom: 1rem;
        color: var(--gray-600);
        line-height: 1.8;
    }

    .key-term {
        background: linear-gradient(135deg, rgba(0, 212, 170, 0.15), rgba(0, 212, 170, 0.05));
        color: var(--teal-dark);
        padding: 0.125rem 0.5rem;
        border-radius: 4px;
        font-weight: 600;
        border-left: 3px solid var(--teal);
    }

    .formula-box {
        background: var(--navy);
        color: var(--white);
        padding: 1.25rem 1.5rem;
        border-radius: var(--radius-md);
        margin: 1.25rem 0;
        font-family: 'Courier New', monospace;
        font-size: 1.1rem;
        text-align: center;
        box-shadow: var(--shadow-md);
    }

    .formula-box small {
        display: block;
        color: var(--gray-400);
        font-size: 0.85rem;
        margin-top: 0.5rem;
    }

    .worked-example {
        background: linear-gradient(135deg, var(--gray-50), var(--white));
        border-left: 4px solid var(--teal);
        padding: 1.25rem 1.5rem;
        border-radius: 0 var(--radius-md) var(--radius-md) 0;
        margin: 1.25rem 0;
    }

    .worked-example strong:first-child {
        display: block;
        color: var(--navy);
        font-size: 1.05rem;
        margin-bottom: 1rem;
        padding-bottom: 0.5rem;
        border-bottom: 1px dashed var(--gray-300);
    }

    .worked-example p {
        margin-bottom: 0.75rem;
    }

    .step-list {
        list-style: none;
        counter-reset: steps;
        margin: 1rem 0;
    }

    .step-list li {
        counter-increment: steps;
        display: flex;
        gap: 1rem;
        margin-bottom: 0.75rem;
        padding: 1rem;
        background: var(--white);
        border-radius: var(--radius-md);
        box-shadow: var(--shadow-sm);
        align-items: flex-start;
    }

    .step-list li::before {
        content: counter(steps);
        min-width: 28px;
        height: 28px;
        background: var(--teal);
        color: var(--navy);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 0.85rem;
        flex-shrink: 0;
    }

    .practice-problems {
        background: var(--gray-100);
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin-top: 1.5rem;
    }

    .practice-problems h4 {
        color: var(--navy);
        margin-bottom: 1rem;
    }

    .practice-problems p {
        padding: 0.5rem 0;
        border-bottom: 1px dashed var(--gray-300);
    }

    .practice-problems p:last-child {
        border-bottom: none;
    }

    /* ========================================
       VIRTUAL LAB SECTION
       ======================================== */
    .lab-zone {
        background: var(--navy);
        border-radius: var(--radius-md);
        padding: 2rem;
        text-align: center;
        min-height: 400px;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        gap: 1.5rem;
    }

    .lab-canvas {
        width: 100%;
        max-width: 700px;
        background: var(--navy-light);
        border-radius: var(--radius-md);
        border: 2px solid var(--gray-500);
        padding: 1.5rem;
    }

    .series-display {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        justify-content: center;
        margin-bottom: 1rem;
        min-height: 60px;
        align-items: center;
    }

    .series-term {
        padding: 0.5rem 0.75rem;
        background: var(--teal);
        color: var(--navy);
        border-radius: var(--radius-sm);
        font-family: 'Courier New', monospace;
        font-weight: 700;
        font-size: 0.95rem;
        opacity: 0;
        transform: translateY(10px);
        transition: all 0.3s ease;
    }

    .series-term.visible {
        opacity: 1;
        transform: translateY(0);
    }

    .series-term.plus {
        background: transparent;
        color: var(--white);
        padding: 0.5rem 0.25rem;
    }

    .sum-display {
        background: var(--navy-dark);
        color: var(--white);
        padding: 1rem 2rem;
        border-radius: var(--radius-md);
        font-family: 'Courier New', monospace;
        font-size: 1.2rem;
        margin-top: 1rem;
    }

    .sum-display .sum-value {
        color: var(--teal-light);
        font-size: 1.5rem;
        font-weight: 700;
    }

    .convergence-indicator {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.75rem 1.5rem;
        border-radius: var(--radius-xl);
        font-weight: 600;
        margin-top: 0.5rem;
    }

    .convergence-indicator.converges {
        background: var(--success-light);
        color: var(--success);
    }

    .convergence-indicator.diverges {
        background: var(--error-light);
        color: var(--error);
    }

    .lab-controls {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
        justify-content: center;
        margin-top: 1rem;
    }

    .slider-container {
        width: 100%;
        max-width: 400px;
        margin: 1rem 0;
    }

    .slider-container label {
        color: var(--white);
        display: block;
        margin-bottom: 0.5rem;
        font-size: 0.9rem;
    }

    .slider-container input[type="range"] {
        width: 100%;
        height: 8px;
        border-radius: 4px;
        background: var(--navy-light);
        outline: none;
        -webkit-appearance: none;
    }

    .slider-container input[type="range"]::-webkit-slider-thumb {
        -webkit-appearance: none;
        width: 20px;
        height: 20px;
        border-radius: 50%;
        background: var(--teal);
        cursor: pointer;
    }

    /* ========================================
       QUIZ SECTION STYLES
       ======================================== */
    .quiz-container p {
        color: var(--gray-600);
        margin-bottom: 1.5rem;
    }

    .quiz-question {
        background: var(--gray-50);
        border-radius: var(--radius-md);
        padding: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .quiz-question h4 {
        color: var(--navy);
        font-size: 1.05rem;
        margin-bottom: 1rem;
    }

    .quiz-options {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .quiz-option {
        padding: 1rem 1.25rem;
        background: var(--white);
        border: 2px solid var(--gray-200);
        border-radius: var(--radius-md);
        cursor: pointer;
        transition: all var(--transition-fast);
        font-size: 0.95rem;
    }

    .quiz-option:hover:not(.disabled) {
        border-color: var(--teal);
        background: rgba(0, 212, 170, 0.05);
    }

    .quiz-option.correct {
        border-color: var(--success);
        background: var(--success-light);
    }

    .quiz-option.incorrect {
        border-color: var(--error);
        background: var(--error-light);
    }

    .quiz-option.disabled {
        cursor: not-allowed;
        opacity: 0.7;
    }

    .quiz-feedback {
        margin-top: 1rem;
        padding: 1rem;
        border-radius: var(--radius-md);
        display: none;
        background: var(--gray-100);
        border-left: 4px solid var(--teal);
    }

    .quiz-feedback.show {
        display: block;
        animation: fadeIn 0.3s ease;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(-10px); }
        to { opacity: 1; transform: translateY(0); }
    }

    .quiz-reset {
        text-align: center;
        margin-top: 1.5rem;
    }

    /* ========================================
       VIDEO SECTION STYLES
       ======================================== */
    .video-container {
        position: relative;
        width: 100%;
        padding-bottom: 56.25%;
        background: var(--navy);
        border-radius: var(--radius-md);
        overflow: hidden;
        margin: 1rem 0;
    }

    .video-container iframe {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        border: none;
    }

    .video-placeholder {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        color: var(--gray-400);
        gap: 1rem;
    }

    .play-btn {
        width: 70px;
        height: 70px;
        background: var(--teal);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all var(--transition-normal);
        box-shadow: 0 0 0 0 rgba(0, 212, 170, 0.4);
        animation: pulse 2s infinite;
    }

    .play-btn:hover {
        transform: scale(1.1);
        background: var(--teal-light);
    }

    @keyframes pulse {
        0% { box-shadow: 0 0 0 0 rgba(0, 212, 170, 0.4); }
        70% { box-shadow: 0 0 0 20px rgba(0, 212, 170, 0); }
        100% { box-shadow: 0 0 0 0 rgba(0, 212, 170, 0); }
    }

    /* ========================================
       TUTOR CTA SECTION
       ======================================== */
    .tutor-cta {
        background: linear-gradient(135deg, var(--navy), var(--navy-light));
        border-radius: var(--radius-lg);
        padding: 2.5rem;
        text-align: center;
        color: var(--white);
        position: relative;
        overflow: hidden;
    }

    .tutor-cta::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -50%;
        width: 100%;
        height: 100%;
        background: radial-gradient(circle, rgba(0, 212, 170, 0.15) 0%, transparent 70%);
    }

    .tutor-cta h3 {
        font-size: 1.5rem;
        margin-bottom: 0.75rem;
        position: relative;
    }

    .tutor-cta p {
        color: var(--gray-300);
        position: relative;
    }

    .tutor-cta a {
        color: var(--teal-light);
    }

    /* ========================================
       BUTTONS
       ======================================== */
    .btn-primary {
        background: linear-gradient(135deg, var(--teal), var(--teal-dark));
        color: var(--navy);
        border: none;
        padding: 0.875rem 2rem;
        border-radius: var(--radius-xl);
        font-family: inherit;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all var(--transition-fast);
        box-shadow: var(--shadow-md);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-lg);
    }

    .btn-secondary {
        background: var(--white);
        color: var(--navy);
        border: 2px solid var(--gray-200);
        padding: 0.75rem 1.5rem;
        border-radius: var(--radius-xl);
        font-family: inherit;
        font-size: 0.9rem;
        font-weight: 600;
        cursor: pointer;
        transition: all var(--transition-fast);
    }

    .btn-secondary:hover {
        border-color: var(--teal);
        color: var(--teal-dark);
    }

    /* ========================================
       FLOATING TUTOR BUTTON
       ======================================== */
    .floating-tutor {
        position: fixed;
        bottom: 90px;
        right: 1.5rem;
        width: 60px;
        height: 60px;
        background: linear-gradient(135deg, var(--teal), var(--teal-dark));
        color: var(--navy);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.75rem;
        cursor: pointer;
        box-shadow: var(--shadow-lg);
        z-index: 998;
        transition: all var(--transition-fast);
    }

    .floating-tutor:hover {
        transform: scale(1.1);
        box-shadow: 0 8px 30px rgba(0, 212, 170, 0.4);
    }

    /* ========================================
       MODAL STYLES
       ======================================== */
    .modal {
        position: fixed;
        inset: 0;
        background: rgba(10, 22, 40, 0.8);
        backdrop-filter: blur(4px);
        z-index: 2000;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1.5rem;
        opacity: 0;
        visibility: hidden;
        transition: all var(--transition-normal);
    }

    .modal.active {
        opacity: 1;
        visibility: visible;
    }

    .modal-content {
        background: var(--white);
        border-radius: var(--radius-lg);
        max-width: 450px;
        width: 100%;
        transform: scale(0.9) translateY(20px);
        transition: all var(--transition-normal);
        overflow: hidden;
    }

    .modal.active .modal-content {
        transform: scale(1) translateY(0);
    }

    .modal-header {
        background: linear-gradient(135deg, var(--navy), var(--navy-light));
        padding: 1.25rem 1.5rem;
        color: var(--white);
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .modal-header h2 {
        font-size: 1.15rem;
    }

    .close-btn {
        background: none;
        border: none;
        color: var(--gray-300);
        font-size: 1.75rem;
        cursor: pointer;
        line-height: 1;
        transition: color var(--transition-fast);
    }

    .close-btn:hover {
        color: var(--white);
    }

    .modal-content > p,
    .modal-content > form {
        padding: 0 1.5rem;
    }

    .modal-content > p {
        padding-top: 1.5rem;
    }

    .modal-content > form {
        padding-bottom: 1.5rem;
    }

    .modal-content input,
    .modal-content textarea {
        width: 100%;
        padding: 0.75rem;
        border: 2px solid var(--gray-200);
        border-radius: var(--radius-md);
        font-family: inherit;
        font-size: 0.95rem;
        transition: border-color var(--transition-fast);
    }

    .modal-content input:focus,
    .modal-content textarea:focus {
        outline: none;
        border-color: var(--teal);
    }

    /* ========================================
       FOOTER NAVIGATION
       ======================================== */
    .lesson-nav {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        background: var(--white);
        box-shadow: 0 -4px 16px rgba(10, 22, 40, 0.1);
        padding: 0.75rem 1.5rem;
        z-index: 997;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .nav-btn {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.625rem 1.25rem;
        border-radius: var(--radius-md);
        font-family: inherit;
        font-size: 0.9rem;
        font-weight: 600;
        cursor: pointer;
        transition: all var(--transition-fast);
        text-decoration: none;
        background: var(--gray-100);
        color: var(--gray-600);
        border: none;
    }

    .nav-btn:hover {
        background: var(--gray-200);
        color: var(--navy);
    }

    .nav-btn.primary {
        background: linear-gradient(135deg, var(--teal), var(--teal-dark));
        color: var(--navy);
        box-shadow: var(--shadow-sm);
    }

    .nav-btn.primary:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-md);
    }

    .nav-center {
        display: flex;
    }

    /* ========================================
       NOTE & WARNING BOXES
       ======================================== */
    .note-box {
        background: linear-gradient(135deg, rgba(0, 212, 170, 0.1), rgba(0, 212, 170, 0.05));
        border: 2px solid var(--teal);
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin: 1.25rem 0;
    }

    .note-box h4 {
        color: var(--teal-dark);
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .note-box p {
        margin-bottom: 0.5rem;
        color: var(--gray-700);
    }

    .warning-box {
        background: linear-gradient(135deg, rgba(239, 68, 68, 0.1), rgba(239, 68, 68, 0.05));
        border: 2px solid var(--error);
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin: 1.25rem 0;
    }

    .warning-box h4 {
        color: var(--error);
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .info-box {
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(59, 130, 246, 0.05));
        border: 2px solid #3b82f6;
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin: 1.25rem 0;
    }

    .info-box h4 {
        color: #3b82f6;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    /* ========================================
       COMPARISON TABLE
       ======================================== */
    .comparison-table {
        width: 100%;
        border-collapse: collapse;
        margin: 1.5rem 0;
        background: var(--white);
        border-radius: var(--radius-md);
        overflow: hidden;
        box-shadow: var(--shadow-sm);
    }

    .comparison-table th {
        background: var(--navy);
        color: var(--white);
        padding: 1rem;
        text-align: left;
        font-weight: 600;
    }

    .comparison-table td {
        padding: 1rem;
        border: 1px solid var(--gray-200);
        vertical-align: top;
    }

    .comparison-table tr:nth-child(even) {
        background: var(--gray-50);
    }

    .comparison-table .converge {
        color: var(--success);
        font-weight: 600;
    }

    .comparison-table .diverge {
        color: var(--error);
        font-weight: 600;
    }

    /* ========================================
       RESPONSIVE STYLES
       ======================================== */
    @media (max-width: 768px) {
        header {
            padding: 0 1rem;
            height: 60px;
        }

        header nav span {
            font-size: 1.1rem !important;
        }

        .sticky-nav {
            top: 60px;
            padding: 0.5rem;
        }

        .lesson-map {
            gap: 0.25rem;
        }

        .nav-link {
            padding: 0.4rem 0.6rem;
            font-size: 0.75rem;
        }

        main {
            margin-top: 115px;
            padding: 1rem;
        }

        .section-header {
            padding: 0.875rem 1rem;
        }

        .section-header h2 {
            font-size: 1rem;
        }

        .section-content {
            padding: 1rem;
        }

        .formula-box {
            font-size: 0.95rem;
            padding: 1rem;
        }

        .worked-example {
            padding: 1rem;
        }

        .lab-zone {
            padding: 1.5rem;
            min-height: 350px;
        }

        .tutor-cta {
            padding: 1.75rem;
        }

        .tutor-cta h3 {
            font-size: 1.25rem;
        }

        .floating-tutor {
            width: 50px;
            height: 50px;
            font-size: 1.5rem;
            bottom: 80px;
            right: 1rem;
        }

        .lesson-nav {
            padding: 0.5rem 1rem;
        }

        .nav-btn {
            padding: 0.5rem 0.875rem;
            font-size: 0.8rem;
        }

        .nav-center .nav-btn span {
            display: none;
        }

        .modal-content {
            margin: 1rem;
        }

        .comparison-table {
            font-size: 0.85rem;
        }

        .comparison-table th,
        .comparison-table td {
            padding: 0.75rem 0.5rem;
        }
    }

    @media (max-width: 480px) {
        header nav span {
            font-size: 0.95rem !important;
        }

        .logo img {
            height: 36px;
        }

        .nav-link {
            padding: 0.35rem 0.5rem;
            font-size: 0.7rem;
        }

        .step-list li {
            flex-direction: column;
            align-items: flex-start;
        }

        .nav-btn {
            padding: 0.5rem 0.75rem;
        }

        .comparison-table {
            display: block;
            overflow-x: auto;
        }

        .series-term {
            font-size: 0.8rem;
            padding: 0.4rem 0.5rem;
        }
    }
</style><style>
    /* ========================================
       CSS VARIABLES - Theme Configuration
       ======================================== */
    :root {
        --navy-dark: #0a1628;
        --navy: #1a2744;
        --navy-light: #2a3a5a;
        --teal: #00d4aa;
        --teal-light: #00f5c4;
        --teal-dark: #00b894;
        --white: #ffffff;
        --gray-50: #f8fafc;
        --gray-100: #f1f5f9;
        --gray-200: #e2e8f0;
        --gray-300: #cbd5e1;
        --gray-400: #94a3b8;
        --gray-500: #64748b;
        --gray-600: #475569;
        --gray-700: #334155;
        --success: #10b981;
        --success-light: #d1fae5;
        --error: #ef4444;
        --error-light: #fee2e2;
        --warning: #f59e0b;
        --warning-light: #fef3c7;
        --shadow-sm: 0 2px 8px rgba(10, 22, 40, 0.08);
        --shadow-md: 0 4px 16px rgba(10, 22, 40, 0.12);
        --shadow-lg: 0 8px 32px rgba(10, 22, 40, 0.16);
        --shadow-neu: 8px 8px 16px rgba(10, 22, 40, 0.1), -8px -8px 16px rgba(255, 255, 255, 0.9);
        --radius-sm: 8px;
        --radius-md: 12px;
        --radius-lg: 16px;
        --radius-xl: 24px;
        --transition-fast: 0.15s ease;
        --transition-normal: 0.3s ease;
    }

    /* ========================================
       BASE STYLES & RESET
       ======================================== */
    *, *::before, *::after {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    html {
        scroll-behavior: smooth;
        scroll-padding-top: 140px;
    }

    body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        background: var(--gray-100);
        color: var(--gray-700);
        line-height: 1.6;
        overflow-x: hidden;
    }

    /* ========================================
       HEADER STYLES
       ======================================== */
    header {
        position: sticky;
        top: 0;
        z-index: 1000;
        background: var(--white);
        box-shadow: var(--shadow-md);
        padding: 1rem 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .logo {
        display: flex;
        align-items: center;
        gap: 12px;
        text-decoration: none;
    }

    .logo img {
        height: 45px;
        width: auto;
        border-radius: var(--radius-sm);
        transition: transform var(--transition-fast);
    }

    .logo:hover img {
        transform: scale(1.05);
    }

    header nav {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    header nav span {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-weight: 700;
        color: var(--navy);
    }

    header nav img {
        height: 28px;
        width: auto;
    }

    /* ========================================
       STICKY LESSON MAP NAVIGATION
       ======================================== */
    .sticky-nav {
        position: fixed;
        top: 70px;
        left: 0;
        right: 0;
        background: var(--navy);
        z-index: 999;
        padding: 0.75rem 1rem;
        box-shadow: var(--shadow-md);
    }

    .lesson-map {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        justify-content: center;
        list-style: none;
        max-width: 1200px;
        margin: 0 auto;
    }

    .lesson-map li {
        flex-shrink: 0;
    }

    .nav-link {
        display: inline-block;
        padding: 0.5rem 1rem;
        color: var(--gray-300);
        text-decoration: none;
        font-size: 0.85rem;
        font-weight: 500;
        border-radius: var(--radius-md);
        transition: all var(--transition-fast);
        white-space: nowrap;
    }

    .nav-link:hover, .nav-link.active {
        color: var(--white);
        background: var(--navy-light);
    }

    /* ========================================
       MAIN CONTENT AREA
       ======================================== */
    main {
        margin-top: 130px;
        padding: 1.5rem;
        max-width: 1000px;
        margin-left: auto;
        margin-right: auto;
        padding-bottom: 100px;
    }

    section {
        background: var(--white);
        border-radius: var(--radius-lg);
        box-shadow: var(--shadow-neu);
        margin-bottom: 1.5rem;
        overflow: hidden;
    }

    /* ========================================
       COLLAPSIBLE SECTION HEADERS
       ======================================== */
    .section-header {
        background: linear-gradient(135deg, var(--navy), var(--navy-light));
        padding: 1rem 1.5rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        cursor: pointer;
        user-select: none;
        transition: background var(--transition-fast);
    }

    .section-header:hover {
        background: linear-gradient(135deg, var(--navy-light), var(--navy));
    }

    .section-header h2 {
        color: var(--white);
        font-size: 1.15rem;
        font-weight: 700;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .toggle-icon {
        color: var(--teal);
        font-size: 1rem;
        transition: transform var(--transition-normal);
    }

    .section-header.collapsed .toggle-icon {
        transform: rotate(-90deg);
    }

    .section-content {
        padding: 1.5rem;
        max-height: 8000px;
        overflow: hidden;
        transition: max-height 0.5s ease, padding 0.3s ease, opacity 0.3s ease;
        opacity: 1;
    }

    .section-content.collapsed {
        max-height: 0;
        padding-top: 0;
        padding-bottom: 0;
        opacity: 0;
    }

    /* ========================================
       OBJECTIVES SECTION
       ======================================== */
    #objectives ul {
        list-style: none;
        margin-top: 1rem;
    }

    #objectives li {
        padding: 0.75rem 1rem;
        padding-left: 2.5rem;
        position: relative;
        background: var(--gray-50);
        border-radius: var(--radius-md);
        margin-bottom: 0.5rem;
        color: var(--gray-700);
        border-left: 4px solid var(--teal);
    }

    #objectives li::before {
        content: '?';
        position: absolute;
        left: 1rem;
        color: var(--teal);
        font-weight: bold;
    }

    /* ========================================
       PRE-KNOWLEDGE & BASELINE SECTIONS
       ======================================== */
    .knowledge-list, .baseline-list {
        list-style: none;
        margin-top: 1rem;
    }

    .knowledge-list li, .baseline-list li {
        padding: 0.75rem 1rem;
        background: var(--gray-50);
        border-radius: var(--radius-md);
        margin-bottom: 0.5rem;
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
    }

    .knowledge-list li::before {
        content: '?';
    }

    .baseline-list li::before {
        content: '?';
    }

    /* ========================================
       THEORY SECTION STYLES
       ======================================== */
    .theory-content h3 {
        color: var(--navy);
        font-size: 1.25rem;
        margin: 1.5rem 0 1rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding-bottom: 0.5rem;
        border-bottom: 2px solid var(--gray-200);
    }

    .theory-content h3:first-child {
        margin-top: 0;
    }

    .theory-content p {
        margin-bottom: 1rem;
        color: var(--gray-600);
        line-height: 1.8;
    }

    .key-term {
        background: linear-gradient(135deg, rgba(0, 212, 170, 0.15), rgba(0, 212, 170, 0.05));
        color: var(--teal-dark);
        padding: 0.125rem 0.5rem;
        border-radius: 4px;
        font-weight: 600;
        border-left: 3px solid var(--teal);
    }

    .formula-box {
        background: var(--navy);
        color: var(--white);
        padding: 1.25rem 1.5rem;
        border-radius: var(--radius-md);
        margin: 1.25rem 0;
        font-family: 'Courier New', monospace;
        font-size: 1.1rem;
        text-align: center;
        box-shadow: var(--shadow-md);
    }

    .formula-box small {
        display: block;
        color: var(--gray-400);
        font-size: 0.85rem;
        margin-top: 0.5rem;
    }

    .worked-example {
        background: linear-gradient(135deg, var(--gray-50), var(--white));
        border-left: 4px solid var(--teal);
        padding: 1.25rem 1.5rem;
        border-radius: 0 var(--radius-md) var(--radius-md) 0;
        margin: 1.25rem 0;
    }

    .worked-example strong:first-child {
        display: block;
        color: var(--navy);
        font-size: 1.05rem;
        margin-bottom: 1rem;
        padding-bottom: 0.5rem;
        border-bottom: 1px dashed var(--gray-300);
    }

    .worked-example p {
        margin-bottom: 0.75rem;
    }

    .step-list {
        list-style: none;
        counter-reset: steps;
        margin: 1rem 0;
    }

    .step-list li {
        counter-increment: steps;
        display: flex;
        gap: 1rem;
        margin-bottom: 0.75rem;
        padding: 1rem;
        background: var(--white);
        border-radius: var(--radius-md);
        box-shadow: var(--shadow-sm);
        align-items: flex-start;
    }

    .step-list li::before {
        content: counter(steps);
        min-width: 28px;
        height: 28px;
        background: var(--teal);
        color: var(--navy);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 0.85rem;
        flex-shrink: 0;
    }

    .practice-problems {
        background: var(--gray-100);
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin-top: 1.5rem;
    }

    .practice-problems h4 {
        color: var(--navy);
        margin-bottom: 1rem;
    }

    .practice-problems p {
        padding: 0.5rem 0;
        border-bottom: 1px dashed var(--gray-300);
    }

    .practice-problems p:last-child {
        border-bottom: none;
    }

    /* ========================================
       VIRTUAL LAB SECTION
       ======================================== */
    .lab-zone {
        background: var(--navy);
        border-radius: var(--radius-md);
        padding: 2rem;
        text-align: center;
        min-height: 400px;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        gap: 1.5rem;
    }

    .lab-canvas {
        width: 100%;
        max-width: 700px;
        background: var(--navy-light);
        border-radius: var(--radius-md);
        border: 2px solid var(--gray-500);
        padding: 1.5rem;
    }

    .series-display {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        justify-content: center;
        margin-bottom: 1rem;
        min-height: 60px;
        align-items: center;
    }

    .series-term {
        padding: 0.5rem 0.75rem;
        background: var(--teal);
        color: var(--navy);
        border-radius: var(--radius-sm);
        font-family: 'Courier New', monospace;
        font-weight: 700;
        font-size: 0.95rem;
        opacity: 0;
        transform: translateY(10px);
        transition: all 0.3s ease;
    }

    .series-term.visible {
        opacity: 1;
        transform: translateY(0);
    }

    .series-term.plus {
        background: transparent;
        color: var(--white);
        padding: 0.5rem 0.25rem;
    }

    .sum-display {
        background: var(--navy-dark);
        color: var(--white);
        padding: 1rem 2rem;
        border-radius: var(--radius-md);
        font-family: 'Courier New', monospace;
        font-size: 1.2rem;
        margin-top: 1rem;
    }

    .sum-display .sum-value {
        color: var(--teal-light);
        font-size: 1.5rem;
        font-weight: 700;
    }

    .convergence-indicator {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.75rem 1.5rem;
        border-radius: var(--radius-xl);
        font-weight: 600;
        margin-top: 0.5rem;
    }

    .convergence-indicator.converges {
        background: var(--success-light);
        color: var(--success);
    }

    .convergence-indicator.diverges {
        background: var(--error-light);
        color: var(--error);
    }

    .lab-controls {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
        justify-content: center;
        margin-top: 1rem;
    }

    .slider-container {
        width: 100%;
        max-width: 400px;
        margin: 1rem 0;
    }

    .slider-container label {
        color: var(--white);
        display: block;
        margin-bottom: 0.5rem;
        font-size: 0.9rem;
    }

    .slider-container input[type="range"] {
        width: 100%;
        height: 8px;
        border-radius: 4px;
        background: var(--navy-light);
        outline: none;
        -webkit-appearance: none;
    }

    .slider-container input[type="range"]::-webkit-slider-thumb {
        -webkit-appearance: none;
        width: 20px;
        height: 20px;
        border-radius: 50%;
        background: var(--teal);
        cursor: pointer;
    }

    /* ========================================
       QUIZ SECTION STYLES
       ======================================== */
    .quiz-container p {
        color: var(--gray-600);
        margin-bottom: 1.5rem;
    }

    .quiz-question {
        background: var(--gray-50);
        border-radius: var(--radius-md);
        padding: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .quiz-question h4 {
        color: var(--navy);
        font-size: 1.05rem;
        margin-bottom: 1rem;
    }

    .quiz-options {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .quiz-option {
        padding: 1rem 1.25rem;
        background: var(--white);
        border: 2px solid var(--gray-200);
        border-radius: var(--radius-md);
        cursor: pointer;
        transition: all var(--transition-fast);
        font-size: 0.95rem;
    }

    .quiz-option:hover:not(.disabled) {
        border-color: var(--teal);
        background: rgba(0, 212, 170, 0.05);
    }

    .quiz-option.correct {
        border-color: var(--success);
        background: var(--success-light);
    }

    .quiz-option.incorrect {
        border-color: var(--error);
        background: var(--error-light);
    }

    .quiz-option.disabled {
        cursor: not-allowed;
        opacity: 0.7;
    }

    .quiz-feedback {
        margin-top: 1rem;
        padding: 1rem;
        border-radius: var(--radius-md);
        display: none;
        background: var(--gray-100);
        border-left: 4px solid var(--teal);
    }

    .quiz-feedback.show {
        display: block;
        animation: fadeIn 0.3s ease;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(-10px); }
        to { opacity: 1; transform: translateY(0); }
    }

    .quiz-reset {
        text-align: center;
        margin-top: 1.5rem;
    }

    /* ========================================
       VIDEO SECTION STYLES
       ======================================== */
    .video-container {
        position: relative;
        width: 100%;
        padding-bottom: 56.25%;
        background: var(--navy);
        border-radius: var(--radius-md);
        overflow: hidden;
        margin: 1rem 0;
    }

    .video-container iframe {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        border: none;
    }

    .video-placeholder {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        color: var(--gray-400);
        gap: 1rem;
    }

    .play-btn {
        width: 70px;
        height: 70px;
        background: var(--teal);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all var(--transition-normal);
        box-shadow: 0 0 0 0 rgba(0, 212, 170, 0.4);
        animation: pulse 2s infinite;
    }

    .play-btn:hover {
        transform: scale(1.1);
        background: var(--teal-light);
    }

    @keyframes pulse {
        0% { box-shadow: 0 0 0 0 rgba(0, 212, 170, 0.4); }
        70% { box-shadow: 0 0 0 20px rgba(0, 212, 170, 0); }
        100% { box-shadow: 0 0 0 0 rgba(0, 212, 170, 0); }
    }

    /* ========================================
       TUTOR CTA SECTION
       ======================================== */
    .tutor-cta {
        background: linear-gradient(135deg, var(--navy), var(--navy-light));
        border-radius: var(--radius-lg);
        padding: 2.5rem;
        text-align: center;
        color: var(--white);
        position: relative;
        overflow: hidden;
    }

    .tutor-cta::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -50%;
        width: 100%;
        height: 100%;
        background: radial-gradient(circle, rgba(0, 212, 170, 0.15) 0%, transparent 70%);
    }

    .tutor-cta h3 {
        font-size: 1.5rem;
        margin-bottom: 0.75rem;
        position: relative;
    }

    .tutor-cta p {
        color: var(--gray-300);
        position: relative;
    }

    .tutor-cta a {
        color: var(--teal-light);
    }

    /* ========================================
       BUTTONS
       ======================================== */
    .btn-primary {
        background: linear-gradient(135deg, var(--teal), var(--teal-dark));
        color: var(--navy);
        border: none;
        padding: 0.875rem 2rem;
        border-radius: var(--radius-xl);
        font-family: inherit;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all var(--transition-fast);
        box-shadow: var(--shadow-md);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-lg);
    }

    .btn-secondary {
        background: var(--white);
        color: var(--navy);
        border: 2px solid var(--gray-200);
        padding: 0.75rem 1.5rem;
        border-radius: var(--radius-xl);
        font-family: inherit;
        font-size: 0.9rem;
        font-weight: 600;
        cursor: pointer;
        transition: all var(--transition-fast);
    }

    .btn-secondary:hover {
        border-color: var(--teal);
        color: var(--teal-dark);
    }

    /* ========================================
       FLOATING TUTOR BUTTON
       ======================================== */
    .floating-tutor {
        position: fixed;
        bottom: 90px;
        right: 1.5rem;
        width: 60px;
        height: 60px;
        background: linear-gradient(135deg, var(--teal), var(--teal-dark));
        color: var(--navy);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.75rem;
        cursor: pointer;
        box-shadow: var(--shadow-lg);
        z-index: 998;
        transition: all var(--transition-fast);
    }

    .floating-tutor:hover {
        transform: scale(1.1);
        box-shadow: 0 8px 30px rgba(0, 212, 170, 0.4);
    }

    /* ========================================
       MODAL STYLES
       ======================================== */
    .modal {
        position: fixed;
        inset: 0;
        background: rgba(10, 22, 40, 0.8);
        backdrop-filter: blur(4px);
        z-index: 2000;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1.5rem;
        opacity: 0;
        visibility: hidden;
        transition: all var(--transition-normal);
    }

    .modal.active {
        opacity: 1;
        visibility: visible;
    }

    .modal-content {
        background: var(--white);
        border-radius: var(--radius-lg);
        max-width: 450px;
        width: 100%;
        transform: scale(0.9) translateY(20px);
        transition: all var(--transition-normal);
        overflow: hidden;
    }

    .modal.active .modal-content {
        transform: scale(1) translateY(0);
    }

    .modal-header {
        background: linear-gradient(135deg, var(--navy), var(--navy-light));
        padding: 1.25rem 1.5rem;
        color: var(--white);
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .modal-header h2 {
        font-size: 1.15rem;
    }

    .close-btn {
        background: none;
        border: none;
        color: var(--gray-300);
        font-size: 1.75rem;
        cursor: pointer;
        line-height: 1;
        transition: color var(--transition-fast);
    }

    .close-btn:hover {
        color: var(--white);
    }

    .modal-content > p,
    .modal-content > form {
        padding: 0 1.5rem;
    }

    .modal-content > p {
        padding-top: 1.5rem;
    }

    .modal-content > form {
        padding-bottom: 1.5rem;
    }

    .modal-content input,
    .modal-content textarea {
        width: 100%;
        padding: 0.75rem;
        border: 2px solid var(--gray-200);
        border-radius: var(--radius-md);
        font-family: inherit;
        font-size: 0.95rem;
        transition: border-color var(--transition-fast);
    }

    .modal-content input:focus,
    .modal-content textarea:focus {
        outline: none;
        border-color: var(--teal);
    }

    /* ========================================
       FOOTER NAVIGATION
       ======================================== */
    .lesson-nav {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        background: var(--white);
        box-shadow: 0 -4px 16px rgba(10, 22, 40, 0.1);
        padding: 0.75rem 1.5rem;
        z-index: 997;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .nav-btn {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.625rem 1.25rem;
        border-radius: var(--radius-md);
        font-family: inherit;
        font-size: 0.9rem;
        font-weight: 600;
        cursor: pointer;
        transition: all var(--transition-fast);
        text-decoration: none;
        background: var(--gray-100);
        color: var(--gray-600);
        border: none;
    }

    .nav-btn:hover {
        background: var(--gray-200);
        color: var(--navy);
    }

    .nav-btn.primary {
        background: linear-gradient(135deg, var(--teal), var(--teal-dark));
        color: var(--navy);
        box-shadow: var(--shadow-sm);
    }

    .nav-btn.primary:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-md);
    }

    .nav-center {
        display: flex;
    }

    /* ========================================
       NOTE & WARNING BOXES
       ======================================== */
    .note-box {
        background: linear-gradient(135deg, rgba(0, 212, 170, 0.1), rgba(0, 212, 170, 0.05));
        border: 2px solid var(--teal);
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin: 1.25rem 0;
    }

    .note-box h4 {
        color: var(--teal-dark);
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .note-box p {
        margin-bottom: 0.5rem;
        color: var(--gray-700);
    }

    .warning-box {
        background: linear-gradient(135deg, rgba(239, 68, 68, 0.1), rgba(239, 68, 68, 0.05));
        border: 2px solid var(--error);
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin: 1.25rem 0;
    }

    .warning-box h4 {
        color: var(--error);
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .info-box {
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(59, 130, 246, 0.05));
        border: 2px solid #3b82f6;
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin: 1.25rem 0;
    }

    .info-box h4 {
        color: #3b82f6;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    /* ========================================
       COMPARISON TABLE
       ======================================== */
    .comparison-table {
        width: 100%;
        border-collapse: collapse;
        margin: 1.5rem 0;
        background: var(--white);
        border-radius: var(--radius-md);
        overflow: hidden;
        box-shadow: var(--shadow-sm);
    }

    .comparison-table th {
        background: var(--navy);
        color: var(--white);
        padding: 1rem;
        text-align: left;
        font-weight: 600;
    }

    .comparison-table td {
        padding: 1rem;
        border: 1px solid var(--gray-200);
        vertical-align: top;
    }

    .comparison-table tr:nth-child(even) {
        background: var(--gray-50);
    }

    .comparison-table .converge {
        color: var(--success);
        font-weight: 600;
    }

    .comparison-table .diverge {
        color: var(--error);
        font-weight: 600;
    }

    /* ========================================
       RESPONSIVE STYLES
       ======================================== */
    @media (max-width: 768px) {
        header {
            padding: 0 1rem;
            height: 60px;
        }

        header nav span {
            font-size: 1.1rem !important;
        }

        .sticky-nav {
            top: 60px;
            padding: 0.5rem;
        }

        .lesson-map {
            gap: 0.25rem;
        }

        .nav-link {
            padding: 0.4rem 0.6rem;
            font-size: 0.75rem;
        }

        main {
            margin-top: 115px;
            padding: 1rem;
        }

        .section-header {
            padding: 0.875rem 1rem;
        }

        .section-header h2 {
            font-size: 1rem;
        }

        .section-content {
            padding: 1rem;
        }

        .formula-box {
            font-size: 0.95rem;
            padding: 1rem;
        }

        .worked-example {
            padding: 1rem;
        }

        .lab-zone {
            padding: 1.5rem;
            min-height: 350px;
        }

        .tutor-cta {
            padding: 1.75rem;
        }

        .tutor-cta h3 {
            font-size: 1.25rem;
        }

        .floating-tutor {
            width: 50px;
            height: 50px;
            font-size: 1.5rem;
            bottom: 80px;
            right: 1rem;
        }

        .lesson-nav {
            padding: 0.5rem 1rem;
        }

        .nav-btn {
            padding: 0.5rem 0.875rem;
            font-size: 0.8rem;
        }

        .nav-center .nav-btn span {
            display: none;
        }

        .modal-content {
            margin: 1rem;
        }

        .comparison-table {
            font-size: 0.85rem;
        }

        .comparison-table th,
        .comparison-table td {
            padding: 0.75rem 0.5rem;
        }
    }

    @media (max-width: 480px) {
        header nav span {
            font-size: 0.95rem !important;
        }

        .logo img {
            height: 36px;
        }

        .nav-link {
            padding: 0.35rem 0.5rem;
            font-size: 0.7rem;
        }

        .step-list li {
            flex-direction: column;
            align-items: flex-start;
        }

        .nav-btn {
            padding: 0.5rem 0.75rem;
        }

        .comparison-table {
            display: block;
            overflow-x: auto;
        }

        .series-term {
            font-size: 0.8rem;
            padding: 0.4rem 0.5rem;
        }
    }
</style><style>
    /* ========================================
       CSS VARIABLES - Theme Configuration
       ======================================== */
    :root {
        --navy-dark: #0a1628;
        --navy: #1a2744;
        --navy-light: #2a3a5a;
        --teal: #00d4aa;
        --teal-light: #00f5c4;
        --teal-dark: #00b894;
        --white: #ffffff;
        --gray-50: #f8fafc;
        --gray-100: #f1f5f9;
        --gray-200: #e2e8f0;
        --gray-300: #cbd5e1;
        --gray-400: #94a3b8;
        --gray-500: #64748b;
        --gray-600: #475569;
        --gray-700: #334155;
        --success: #10b981;
        --success-light: #d1fae5;
        --error: #ef4444;
        --error-light: #fee2e2;
        --warning: #f59e0b;
        --warning-light: #fef3c7;
        --shadow-sm: 0 2px 8px rgba(10, 22, 40, 0.08);
        --shadow-md: 0 4px 16px rgba(10, 22, 40, 0.12);
        --shadow-lg: 0 8px 32px rgba(10, 22, 40, 0.16);
        --shadow-neu: 8px 8px 16px rgba(10, 22, 40, 0.1), -8px -8px 16px rgba(255, 255, 255, 0.9);
        --radius-sm: 8px;
        --radius-md: 12px;
        --radius-lg: 16px;
        --radius-xl: 24px;
        --transition-fast: 0.15s ease;
        --transition-normal: 0.3s ease;
    }

    /* ========================================
       BASE STYLES & RESET
       ======================================== */
    *, *::before, *::after {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    html {
        scroll-behavior: smooth;
        scroll-padding-top: 140px;
    }

    body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        background: var(--gray-100);
        color: var(--gray-700);
        line-height: 1.6;
        overflow-x: hidden;
    }

    /* ========================================
       HEADER STYLES
       ======================================== */
    header {
        position: sticky;
        top: 0;
        z-index: 1000;
        background: var(--white);
        box-shadow: var(--shadow-md);
        padding: 1rem 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .logo {
        display: flex;
        align-items: center;
        gap: 12px;
        text-decoration: none;
    }

    .logo img {
        height: 45px;
        width: auto;
        border-radius: var(--radius-sm);
        transition: transform var(--transition-fast);
    }

    .logo:hover img {
        transform: scale(1.05);
    }

    header nav {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    header nav span {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-weight: 700;
        color: var(--navy);
    }

    header nav img {
        height: 28px;
        width: auto;
    }

    /* ========================================
       STICKY LESSON MAP NAVIGATION
       ======================================== */
    .sticky-nav {
        position: fixed;
        top: 70px;
        left: 0;
        right: 0;
        background: var(--navy);
        z-index: 999;
        padding: 0.75rem 1rem;
        box-shadow: var(--shadow-md);
    }

    .lesson-map {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        justify-content: center;
        list-style: none;
        max-width: 1200px;
        margin: 0 auto;
    }

    .lesson-map li {
        flex-shrink: 0;
    }

    .nav-link {
        display: inline-block;
        padding: 0.5rem 1rem;
        color: var(--gray-300);
        text-decoration: none;
        font-size: 0.85rem;
        font-weight: 500;
        border-radius: var(--radius-md);
        transition: all var(--transition-fast);
        white-space: nowrap;
    }

    .nav-link:hover, .nav-link.active {
        color: var(--white);
        background: var(--navy-light);
    }

    /* ========================================
       MAIN CONTENT AREA
       ======================================== */
    main {
        margin-top: 130px;
        padding: 1.5rem;
        max-width: 1000px;
        margin-left: auto;
        margin-right: auto;
        padding-bottom: 100px;
    }

    section {
        background: var(--white);
        border-radius: var(--radius-lg);
        box-shadow: var(--shadow-neu);
        margin-bottom: 1.5rem;
        overflow: hidden;
    }

    /* ========================================
       COLLAPSIBLE SECTION HEADERS
       ======================================== */
    .section-header {
        background: linear-gradient(135deg, var(--navy), var(--navy-light));
        padding: 1rem 1.5rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        cursor: pointer;
        user-select: none;
        transition: background var(--transition-fast);
    }

    .section-header:hover {
        background: linear-gradient(135deg, var(--navy-light), var(--navy));
    }

    .section-header h2 {
        color: var(--white);
        font-size: 1.15rem;
        font-weight: 700;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .toggle-icon {
        color: var(--teal);
        font-size: 1rem;
        transition: transform var(--transition-normal);
    }

    .section-header.collapsed .toggle-icon {
        transform: rotate(-90deg);
    }

    .section-content {
        padding: 1.5rem;
        max-height: 8000px;
        overflow: hidden;
        transition: max-height 0.5s ease, padding 0.3s ease, opacity 0.3s ease;
        opacity: 1;
    }

    .section-content.collapsed {
        max-height: 0;
        padding-top: 0;
        padding-bottom: 0;
        opacity: 0;
    }

    /* ========================================
       OBJECTIVES SECTION
       ======================================== */
    #objectives ul {
        list-style: none;
        margin-top: 1rem;
    }

    #objectives li {
        padding: 0.75rem 1rem;
        padding-left: 2.5rem;
        position: relative;
        background: var(--gray-50);
        border-radius: var(--radius-md);
        margin-bottom: 0.5rem;
        color: var(--gray-700);
        border-left: 4px solid var(--teal);
    }

    #objectives li::before {
        content: '?';
        position: absolute;
        left: 1rem;
        color: var(--teal);
        font-weight: bold;
    }

    /* ========================================
       PRE-KNOWLEDGE & BASELINE SECTIONS
       ======================================== */
    .knowledge-list, .baseline-list {
        list-style: none;
        margin-top: 1rem;
    }

    .knowledge-list li, .baseline-list li {
        padding: 0.75rem 1rem;
        background: var(--gray-50);
        border-radius: var(--radius-md);
        margin-bottom: 0.5rem;
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
    }

    .knowledge-list li::before {
        content: '?';
    }

    .baseline-list li::before {
        content: '?';
    }

    /* ========================================
       THEORY SECTION STYLES
       ======================================== */
    .theory-content h3 {
        color: var(--navy);
        font-size: 1.25rem;
        margin: 1.5rem 0 1rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding-bottom: 0.5rem;
        border-bottom: 2px solid var(--gray-200);
    }

    .theory-content h3:first-child {
        margin-top: 0;
    }

    .theory-content p {
        margin-bottom: 1rem;
        color: var(--gray-600);
        line-height: 1.8;
    }

    .key-term {
        background: linear-gradient(135deg, rgba(0, 212, 170, 0.15), rgba(0, 212, 170, 0.05));
        color: var(--teal-dark);
        padding: 0.125rem 0.5rem;
        border-radius: 4px;
        font-weight: 600;
        border-left: 3px solid var(--teal);
    }

    .formula-box {
        background: var(--navy);
        color: var(--white);
        padding: 1.25rem 1.5rem;
        border-radius: var(--radius-md);
        margin: 1.25rem 0;
        font-family: 'Courier New', monospace;
        font-size: 1.1rem;
        text-align: center;
        box-shadow: var(--shadow-md);
    }

    .formula-box small {
        display: block;
        color: var(--gray-400);
        font-size: 0.85rem;
        margin-top: 0.5rem;
    }

    .worked-example {
        background: linear-gradient(135deg, var(--gray-50), var(--white));
        border-left: 4px solid var(--teal);
        padding: 1.25rem 1.5rem;
        border-radius: 0 var(--radius-md) var(--radius-md) 0;
        margin: 1.25rem 0;
    }

    .worked-example strong:first-child {
        display: block;
        color: var(--navy);
        font-size: 1.05rem;
        margin-bottom: 1rem;
        padding-bottom: 0.5rem;
        border-bottom: 1px dashed var(--gray-300);
    }

    .worked-example p {
        margin-bottom: 0.75rem;
    }

    .step-list {
        list-style: none;
        counter-reset: steps;
        margin: 1rem 0;
    }

    .step-list li {
        counter-increment: steps;
        display: flex;
        gap: 1rem;
        margin-bottom: 0.75rem;
        padding: 1rem;
        background: var(--white);
        border-radius: var(--radius-md);
        box-shadow: var(--shadow-sm);
        align-items: flex-start;
    }

    .step-list li::before {
        content: counter(steps);
        min-width: 28px;
        height: 28px;
        background: var(--teal);
        color: var(--navy);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
        font-size: 0.85rem;
        flex-shrink: 0;
    }

    .practice-problems {
        background: var(--gray-100);
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin-top: 1.5rem;
    }

    .practice-problems h4 {
        color: var(--navy);
        margin-bottom: 1rem;
    }

    .practice-problems p {
        padding: 0.5rem 0;
        border-bottom: 1px dashed var(--gray-300);
    }

    .practice-problems p:last-child {
        border-bottom: none;
    }

    /* ========================================
       VIRTUAL LAB SECTION
       ======================================== */
    .lab-zone {
        background: var(--navy);
        border-radius: var(--radius-md);
        padding: 2rem;
        text-align: center;
        min-height: 400px;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        gap: 1.5rem;
    }

    .lab-canvas {
        width: 100%;
        max-width: 700px;
        background: var(--navy-light);
        border-radius: var(--radius-md);
        border: 2px solid var(--gray-500);
        padding: 1.5rem;
    }

    .series-display {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        justify-content: center;
        margin-bottom: 1rem;
        min-height: 60px;
        align-items: center;
    }

    .series-term {
        padding: 0.5rem 0.75rem;
        background: var(--teal);
        color: var(--navy);
        border-radius: var(--radius-sm);
        font-family: 'Courier New', monospace;
        font-weight: 700;
        font-size: 0.95rem;
        opacity: 0;
        transform: translateY(10px);
        transition: all 0.3s ease;
    }

    .series-term.visible {
        opacity: 1;
        transform: translateY(0);
    }

    .series-term.plus {
        background: transparent;
        color: var(--white);
        padding: 0.5rem 0.25rem;
    }

    .sum-display {
        background: var(--navy-dark);
        color: var(--white);
        padding: 1rem 2rem;
        border-radius: var(--radius-md);
        font-family: 'Courier New', monospace;
        font-size: 1.2rem;
        margin-top: 1rem;
    }

    .sum-display .sum-value {
        color: var(--teal-light);
        font-size: 1.5rem;
        font-weight: 700;
    }

    .convergence-indicator {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.75rem 1.5rem;
        border-radius: var(--radius-xl);
        font-weight: 600;
        margin-top: 0.5rem;
    }

    .convergence-indicator.converges {
        background: var(--success-light);
        color: var(--success);
    }

    .convergence-indicator.diverges {
        background: var(--error-light);
        color: var(--error);
    }

    .lab-controls {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
        justify-content: center;
        margin-top: 1rem;
    }

    .slider-container {
        width: 100%;
        max-width: 400px;
        margin: 1rem 0;
    }

    .slider-container label {
        color: var(--white);
        display: block;
        margin-bottom: 0.5rem;
        font-size: 0.9rem;
    }

    .slider-container input[type="range"] {
        width: 100%;
        height: 8px;
        border-radius: 4px;
        background: var(--navy-light);
        outline: none;
        -webkit-appearance: none;
    }

    .slider-container input[type="range"]::-webkit-slider-thumb {
        -webkit-appearance: none;
        width: 20px;
        height: 20px;
        border-radius: 50%;
        background: var(--teal);
        cursor: pointer;
    }

    /* ========================================
       QUIZ SECTION STYLES
       ======================================== */
    .quiz-container p {
        color: var(--gray-600);
        margin-bottom: 1.5rem;
    }

    .quiz-question {
        background: var(--gray-50);
        border-radius: var(--radius-md);
        padding: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .quiz-question h4 {
        color: var(--navy);
        font-size: 1.05rem;
        margin-bottom: 1rem;
    }

    .quiz-options {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .quiz-option {
        padding: 1rem 1.25rem;
        background: var(--white);
        border: 2px solid var(--gray-200);
        border-radius: var(--radius-md);
        cursor: pointer;
        transition: all var(--transition-fast);
        font-size: 0.95rem;
    }

    .quiz-option:hover:not(.disabled) {
        border-color: var(--teal);
        background: rgba(0, 212, 170, 0.05);
    }

    .quiz-option.correct {
        border-color: var(--success);
        background: var(--success-light);
    }

    .quiz-option.incorrect {
        border-color: var(--error);
        background: var(--error-light);
    }

    .quiz-option.disabled {
        cursor: not-allowed;
        opacity: 0.7;
    }

    .quiz-feedback {
        margin-top: 1rem;
        padding: 1rem;
        border-radius: var(--radius-md);
        display: none;
        background: var(--gray-100);
        border-left: 4px solid var(--teal);
    }

    .quiz-feedback.show {
        display: block;
        animation: fadeIn 0.3s ease;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(-10px); }
        to { opacity: 1; transform: translateY(0); }
    }

    .quiz-reset {
        text-align: center;
        margin-top: 1.5rem;
    }

    /* ========================================
       VIDEO SECTION STYLES
       ======================================== */
    .video-container {
        position: relative;
        width: 100%;
        padding-bottom: 56.25%;
        background: var(--navy);
        border-radius: var(--radius-md);
        overflow: hidden;
        margin: 1rem 0;
    }

    .video-container iframe {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        border: none;
    }

    .video-placeholder {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        color: var(--gray-400);
        gap: 1rem;
    }

    .play-btn {
        width: 70px;
        height: 70px;
        background: var(--teal);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all var(--transition-normal);
        box-shadow: 0 0 0 0 rgba(0, 212, 170, 0.4);
        animation: pulse 2s infinite;
    }

    .play-btn:hover {
        transform: scale(1.1);
        background: var(--teal-light);
    }

    @keyframes pulse {
        0% { box-shadow: 0 0 0 0 rgba(0, 212, 170, 0.4); }
        70% { box-shadow: 0 0 0 20px rgba(0, 212, 170, 0); }
        100% { box-shadow: 0 0 0 0 rgba(0, 212, 170, 0); }
    }

    /* ========================================
       TUTOR CTA SECTION
       ======================================== */
    .tutor-cta {
        background: linear-gradient(135deg, var(--navy), var(--navy-light));
        border-radius: var(--radius-lg);
        padding: 2.5rem;
        text-align: center;
        color: var(--white);
        position: relative;
        overflow: hidden;
    }

    .tutor-cta::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -50%;
        width: 100%;
        height: 100%;
        background: radial-gradient(circle, rgba(0, 212, 170, 0.15) 0%, transparent 70%);
    }

    .tutor-cta h3 {
        font-size: 1.5rem;
        margin-bottom: 0.75rem;
        position: relative;
    }

    .tutor-cta p {
        color: var(--gray-300);
        position: relative;
    }

    .tutor-cta a {
        color: var(--teal-light);
    }

    /* ========================================
       BUTTONS
       ======================================== */
    .btn-primary {
        background: linear-gradient(135deg, var(--teal), var(--teal-dark));
        color: var(--navy);
        border: none;
        padding: 0.875rem 2rem;
        border-radius: var(--radius-xl);
        font-family: inherit;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all var(--transition-fast);
        box-shadow: var(--shadow-md);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-lg);
    }

    .btn-secondary {
        background: var(--white);
        color: var(--navy);
        border: 2px solid var(--gray-200);
        padding: 0.75rem 1.5rem;
        border-radius: var(--radius-xl);
        font-family: inherit;
        font-size: 0.9rem;
        font-weight: 600;
        cursor: pointer;
        transition: all var(--transition-fast);
    }

    .btn-secondary:hover {
        border-color: var(--teal);
        color: var(--teal-dark);
    }

    /* ========================================
       FLOATING TUTOR BUTTON
       ======================================== */
    .floating-tutor {
        position: fixed;
        bottom: 90px;
        right: 1.5rem;
        width: 60px;
        height: 60px;
        background: linear-gradient(135deg, var(--teal), var(--teal-dark));
        color: var(--navy);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.75rem;
        cursor: pointer;
        box-shadow: var(--shadow-lg);
        z-index: 998;
        transition: all var(--transition-fast);
    }

    .floating-tutor:hover {
        transform: scale(1.1);
        box-shadow: 0 8px 30px rgba(0, 212, 170, 0.4);
    }

    /* ========================================
       MODAL STYLES
       ======================================== */
    .modal {
        position: fixed;
        inset: 0;
        background: rgba(10, 22, 40, 0.8);
        backdrop-filter: blur(4px);
        z-index: 2000;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1.5rem;
        opacity: 0;
        visibility: hidden;
        transition: all var(--transition-normal);
    }

    .modal.active {
        opacity: 1;
        visibility: visible;
    }

    .modal-content {
        background: var(--white);
        border-radius: var(--radius-lg);
        max-width: 450px;
        width: 100%;
        transform: scale(0.9) translateY(20px);
        transition: all var(--transition-normal);
        overflow: hidden;
    }

    .modal.active .modal-content {
        transform: scale(1) translateY(0);
    }

    .modal-header {
        background: linear-gradient(135deg, var(--navy), var(--navy-light));
        padding: 1.25rem 1.5rem;
        color: var(--white);
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .modal-header h2 {
        font-size: 1.15rem;
    }

    .close-btn {
        background: none;
        border: none;
        color: var(--gray-300);
        font-size: 1.75rem;
        cursor: pointer;
        line-height: 1;
        transition: color var(--transition-fast);
    }

    .close-btn:hover {
        color: var(--white);
    }

    .modal-content > p,
    .modal-content > form {
        padding: 0 1.5rem;
    }

    .modal-content > p {
        padding-top: 1.5rem;
    }

    .modal-content > form {
        padding-bottom: 1.5rem;
    }

    .modal-content input,
    .modal-content textarea {
        width: 100%;
        padding: 0.75rem;
        border: 2px solid var(--gray-200);
        border-radius: var(--radius-md);
        font-family: inherit;
        font-size: 0.95rem;
        transition: border-color var(--transition-fast);
    }

    .modal-content input:focus,
    .modal-content textarea:focus {
        outline: none;
        border-color: var(--teal);
    }

    /* ========================================
       FOOTER NAVIGATION
       ======================================== */
    .lesson-nav {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        background: var(--white);
        box-shadow: 0 -4px 16px rgba(10, 22, 40, 0.1);
        padding: 0.75rem 1.5rem;
        z-index: 997;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .nav-btn {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.625rem 1.25rem;
        border-radius: var(--radius-md);
        font-family: inherit;
        font-size: 0.9rem;
        font-weight: 600;
        cursor: pointer;
        transition: all var(--transition-fast);
        text-decoration: none;
        background: var(--gray-100);
        color: var(--gray-600);
        border: none;
    }

    .nav-btn:hover {
        background: var(--gray-200);
        color: var(--navy);
    }

    .nav-btn.primary {
        background: linear-gradient(135deg, var(--teal), var(--teal-dark));
        color: var(--navy);
        box-shadow: var(--shadow-sm);
    }

    .nav-btn.primary:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-md);
    }

    .nav-center {
        display: flex;
    }

    /* ========================================
       NOTE & WARNING BOXES
       ======================================== */
    .note-box {
        background: linear-gradient(135deg, rgba(0, 212, 170, 0.1), rgba(0, 212, 170, 0.05));
        border: 2px solid var(--teal);
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin: 1.25rem 0;
    }

    .note-box h4 {
        color: var(--teal-dark);
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .note-box p {
        margin-bottom: 0.5rem;
        color: var(--gray-700);
    }

    .warning-box {
        background: linear-gradient(135deg, rgba(239, 68, 68, 0.1), rgba(239, 68, 68, 0.05));
        border: 2px solid var(--error);
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin: 1.25rem 0;
    }

    .warning-box h4 {
        color: var(--error);
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .info-box {
        background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(59, 130, 246, 0.05));
        border: 2px solid #3b82f6;
        border-radius: var(--radius-md);
        padding: 1.25rem;
        margin: 1.25rem 0;
    }

    .info-box h4 {
        color: #3b82f6;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    /* ========================================
       COMPARISON TABLE
       ======================================== */
    .comparison-table {
        width: 100%;
        border-collapse: collapse;
        margin: 1.5rem 0;
        background: var(--white);
        border-radius: var(--radius-md);
        overflow: hidden;
        box-shadow: var(--shadow-sm);
    }

    .comparison-table th {
        background: var(--navy);
        color: var(--white);
        padding: 1rem;
        text-align: left;
        font-weight: 600;
    }

    .comparison-table td {
        padding: 1rem;
        border: 1px solid var(--gray-200);
        vertical-align: top;
    }

    .comparison-table tr:nth-child(even) {
        background: var(--gray-50);
    }

    .comparison-table .converge {
        color: var(--success);
        font-weight: 600;
    }

    .comparison-table .diverge {
        color: var(--error);
        font-weight: 600;
    }

    /* ========================================
       RESPONSIVE STYLES
       ======================================== */
    @media (max-width: 768px) {
        header {
            padding: 0 1rem;
            height: 60px;
        }

        header nav span {
            font-size: 1.1rem !important;
        }

        .sticky-nav {
            top: 60px;
            padding: 0.5rem;
        }

        .lesson-map {
            gap: 0.25rem;
        }

        .nav-link {
            padding: 0.4rem 0.6rem;
            font-size: 0.75rem;
        }

        main {
            margin-top: 115px;
            padding: 1rem;
        }

        .section-header {
            padding: 0.875rem 1rem;
        }

        .section-header h2 {
            font-size: 1rem;
        }

        .section-content {
            padding: 1rem;
        }

        .formula-box {
            font-size: 0.95rem;
            padding: 1rem;
        }

        .worked-example {
            padding: 1rem;
        }

        .lab-zone {
            padding: 1.5rem;
            min-height: 350px;
        }

        .tutor-cta {
            padding: 1.75rem;
        }

        .tutor-cta h3 {
            font-size: 1.25rem;
        }

        .floating-tutor {
            width: 50px;
            height: 50px;
            font-size: 1.5rem;
            bottom: 80px;
            right: 1rem;
        }

        .lesson-nav {
            padding: 0.5rem 1rem;
        }

        .nav-btn {
            padding: 0.5rem 0.875rem;
            font-size: 0.8rem;
        }

        .nav-center .nav-btn span {
            display: none;
        }

        .modal-content {
            margin: 1rem;
        }

        .comparison-table {
            font-size: 0.85rem;
        }

        .comparison-table th,
        .comparison-table td {
            padding: 0.75rem 0.5rem;
        }
    }

    @media (max-width: 480px) {
        header nav span {
            font-size: 0.95rem !important;
        }

        .logo img {
            height: 36px;
        }

        .nav-link {
            padding: 0.35rem 0.5rem;
            font-size: 0.7rem;
        }

        .step-list li {
            flex-direction: column;
            align-items: flex-start;
        }

        .nav-btn {
            padding: 0.5rem 0.75rem;
        }

        .comparison-table {
            display: block;
            overflow-x: auto;
        }

        .series-term {
            font-size: 0.8rem;
            padding: 0.4rem 0.5rem;
        }
    }
</style></head> <body> <!-- ===== HEADER ===== --> <header> <a href="index.html" class="logo"> <img src="../logo_tick.jpg" alt="PassProof Academy Logo"> </a> <nav style="flex: 1; text-align: right;"> <span style="font-size: 2.2rem; color: #0f0909;"><img src="../favicon-32x32.png" alt="PassProof Academy Logo">Grade 12 Mathematics</span> </nav> </header> 
<!-- ===== STICKY LESSON MAP ===== --> <nav class="sticky-nav"> <ul class="lesson-map"> <li><a href="#objectives" class="nav-link">? Objectives</a></li> <li><a href="#pre-knowledge" class="nav-link">? Pre-Knowledge</a></li> <li><a href="#baseline" class="nav-link">? Baseline</a></li> <li><a href="#convergence" class="nav-link">? Convergence</a></li> <li><a href="#infinity" class="nav-link">? Sum to Infinity</a></li> <li><a href="#examples" class="nav-link">? Examples</a></li> <li><a href="#lab" class="nav-link">? Virtual Lab</a></li> <li><a href="#quiz" class="nav-link">? Quiz</a></li> <li><a href="#video" class="nav-link">? Video</a></li> <li><a href="#tutor" class="nav-link">??? Live Tutor</a></li> </ul> </nav> <!-- ===== MAIN CONTENT ===== --> <main> <!-- Lesson Header --> <section style="background: linear-gradient(135deg, var(--navy), var(--navy-light)); color: var(--white); padding: 1.5rem; border-radius: var(--radius-lg); margin-bottom: 1.5rem;"> <h1 style="font-size: 1.5rem; margin-bottom: 0.5rem;">Infinite Geometric Series</h1> <div style="display: flex; flex-wrap: wrap; gap: 1rem; margin-top: 1rem;"> <div style="background: rgba(255,255,255,0.1); padding: 0.5rem 1rem; border-radius: var(--radius-md);"> <strong>Grade:</strong> 12 </div> <div style="background: rgba(255,255,255,0.1); padding: 0.5rem 1rem; border-radius: var(--radius-md);"> <strong>Week:</strong> Term 1 Week 3 </div> <div style="background: rgba(255,255,255,0.1); padding: 0.5rem 1rem; border-radius: var(--radius-md);"> <strong>Time:</strong> 60 minutes </div> <div style="background: rgba(255,255,255,0.1); padding: 0.5rem 1rem; border-radius: var(--radius-md);"> <strong>Topic:</strong> Series - Convergence & Sum to Infinity </div> </div> <p style="margin-top: 1rem; color: var(--gray-300);"> Discover when an infinite geometric series has a finite sum, and learn to calculate the sum to infinity using the formula S? = a/(1-r). </p> </section> <!-- Section: Objectives --> <section id="objectives"> <div class="section-header" data-toggle="objectives-content"> <h2>? Lesson Objectives</h2> <span class="toggle-icon">?</span> </div> <div class="section-content" id="objectives-content"> <p>This lesson introduces the concept of infinite geometric series. By the end of this lesson, you will be able to:</p> <ul> <li>Distinguish between <strong>diverging</strong> and <strong>converging</strong> geometric series</li> <li>Investigate the sum of an infinite geometric series</li> <li>Identify a converging geometric series and understand the condition: <strong>-1 < r < 1</strong></li> <li>Apply the formula S? = a/(1-r) to sum a converging geometric series to infinity</li> <li>Determine whether a given series converges or diverges based on the common ratio</li> </ul> </div> </section> <!-- Section: Pre-Knowledge --> <section id="pre-knowledge"> <div class="section-header" data-toggle="pre-knowledge-content"> <h2>? Pre-Knowledge Required</h2> <span class="toggle-icon">?</span> </div> <div class="section-content" id="pre-knowledge-content"> <p>Before starting this lesson, you should be familiar with:</p> <ul class="knowledge-list"> <li>The general term of a geometric sequence: <strong>T? = a·r??¹</strong></li> <li>The sum formula for a finite geometric series: <strong>S? = a(r? - 1)/(r - 1)</strong></li> <li>Finding the common ratio r by dividing consecutive terms</li> <li>Understanding what happens when you multiply by numbers less than 1</li> <li>The concept of limits and what it means for a value to "approach" something</li> </ul> </div> </section> <!-- Section: Baseline Questions --> <section id="baseline"> <div class="section-header" data-toggle="baseline-content"> <h2>? Baseline Questions</h2> <span class="toggle-icon">?</span> </div> <div class="section-content" id="baseline-content"> <p>Review your understanding of finite geometric series:</p> <ul class="baseline-list"> <li>What is the sum formula for a geometric series with n terms?</li> <li>For the series 3 + 6 + 12 + 24 + ..., what is the common ratio r?</li> <li>If you keep multiplying 100 by 0.5, what happens to the values?</li> <li>What happens to (0.5)? as n gets very large (approaches infinity)?</li> <li>Can an arithmetic series ever converge to a finite sum? Why or why not?</li> </ul> </div> </section> <!-- Section: Convergence vs Divergence --> <section id="convergence"> <div class="section-header" data-toggle="convergence-content"> <h2>? Diverging vs Converging Series</h2> <span class="toggle-icon">?</span> </div> <div class="section-content theory-content" id="convergence-content"> <h3><span class="key-term">What Happens When We Add Infinite Terms?</span></h3> <p> Up until now, we've been finding the sum of a <strong>finite (fixed) number</strong> of terms. But what happens if the series goes on forever? Can we still find a sum? </p> <p> An <span class="key-term">infinite series</span> is one with no last term—it continues without ending. The behavior of such a series depends entirely on the value of the common ratio <strong>r</strong>. </p> <h3><span class="key-term">Examples of Different Series Behaviors</span></h3> <table class="comparison-table"> <thead> <tr> <th>Series</th> <th>Common Ratio (r)</th> <th>Behavior</th> <th>Result</th> </tr> </thead> <tbody> <tr> <td>1 + 3 + 9 + 27 + ...</td> <td>r = 3</td> <td class="diverge">r > 1</td> <td>DIVERGES (sum gets bigger forever)</td> </tr> <tr> <td>1 - 2 + 4 - 8 + ...</td> <td>r = -2</td> <td class="diverge">r < -1</td> <td>DIVERGES (sum oscillates wildly)</td> </tr> <tr> <td>2 + 2 + 2 + 2 + ...</td> <td>r = 1</td> <td class="diverge">r = 1</td> <td>DIVERGES (sum grows without bound)</td> </tr> <tr> <td>2 - 2 + 2 - 2 + ...</td> <td>r = -1</td> <td class="diverge">r = -1</td> <td>DIVERGES (sum alternates 2 or 0)</td> </tr> <tr> <td>1 + ½ + ¼ + ? + ...</td> <td>r = ½</td> <td class="converge">-1 < r < 1</td> <td><strong>CONVERGES to 2</strong></td> </tr> </tbody> </table> <div class="note-box"> <h4>? Key Insight: The Converging Series</h4> <p>Look at the last example: 1 + ½ + ¼ + ? + 1/16 + ...</p> <p><strong>S? = 1</strong> (just the first term)</p> <p><strong>S? = 1.5</strong> (1 + 0.5)</p> <p><strong>S? = 1.75</strong> (1 + 0.5 + 0.25)</p> <p><strong>S? = 1.875</strong> (getting closer to 2...)</p> <p><strong>S?? ? 1.998</strong> (almost at 2!)</p> <p>As we add more terms, the sum gets closer and closer to 2, but never exceeds it!</p> </div> <div class="formula-box" style="background: linear-gradient(135deg, var(--success), #059669);"> <strong>CONVERGENCE CONDITION</strong><br> A geometric series CONVERGES (has a finite sum) if and only if:<br><br> <span style="font-size: 1.3rem;">-1 < r < 1</span> (equivalently: |r| < 1) </div> <div class="warning-box"> <h4>?? Important: Arithmetic Series NEVER Converge</h4> <p>An arithmetic series (with constant difference d ? 0) will always diverge because we keep adding the same amount forever. Only geometric series with |r| < 1 can converge.</p> </div> </div> </section> <!-- Section: Sum to Infinity --> <section id="infinity"> <div class="section-header" data-toggle="infinity-content"> <h2>? Sum to Infinity Formula</h2> <span class="toggle-icon">?</span> </div> <div class="section-content theory-content" id="infinity-content"> <h3><span class="key-term">Deriving the Sum to Infinity Formula</span></h3> <p> Recall the sum formula for n terms: <strong>S? = a(1 - r?)/(1 - r)</strong> </p> <p> When <strong>|r| < 1</strong>, something special happens as n ? ?: </p> <div class="worked-example"> <strong>What happens to r? when |r| < 1?</strong> <p>Let's take r = 0.5 (which is between -1 and 1):</p> <ul class="step-list"> <li>(0.5)¹ = 0.5</li> <li>(0.5)² = 0.25</li> <li>(0.5)? = 0.03125</li> <li>(0.5)¹? = 0.0009765625</li> <li>(0.5)²? = 0.00000095... (almost zero!)</li> </ul> <p style="margin-top: 1rem; font-weight: 600; color: var(--teal-dark);"> As n ? ?, r? ? 0 when |r| < 1 </p> </div> <p> So in our formula S? = a(1 - r?)/(1 - r), as n approaches infinity: </p> <ul> <li>r? approaches 0</li> <li>Therefore (1 - r?) approaches (1 - 0) = 1</li> <li>And S? approaches a/(1 - r)</li> </ul> <div class="formula-box"> <strong>Sum to Infinity Formula</strong><br><br> <span style="font-size: 1.4rem;">S? = a / (1 - r)</span><br><br> <small>Valid ONLY when: -1 < r < 1 (i.e., |r| < 1)</small> </div> <div class="note-box"> <h4>? Memory Tip</h4> <p><strong>"a over (1 minus r)"</strong> - Remember: the first term 'a' divided by (1 - r)</p> <p>Always check that |r| < 1 before using this formula!</p> </div> <div class="info-box"> <h4>? Alternative Notation</h4> <p>The condition -1 < r < 1 is sometimes written as:</p> <p>• |r| < 1 (absolute value of r is less than 1)</p> <p>• r ? (-1, 1) (r is in the open interval from -1 to 1)</p> </div> </div> </section> <!-- Section: Worked Examples --> <section id="examples"> <div class="section-header" data-toggle="examples-content"> <h2>? Worked Examples</h2> <span class="toggle-icon">?</span> </div> <div class="section-content theory-content" id="examples-content"> <h3><span class="key-term">Example 1: Simple Sum to Infinity</span></h3> <div class="worked-example"> <strong>Find the sum to infinity of: 10 + 1 + 0.1 + 0.01 + ...</strong> <ul class="step-list"> <li> <div> <strong>Identify a and r:</strong><br> First term: a = 10<br> Common ratio: r = 1/10 = 0.1 </div> </li> <li> <div> <strong>Check convergence condition:</strong><br> Is -1 < r < 1?<br> Is -1 < 0.1 < 1? ? <strong>YES - the series converges!</strong> </div> </li> <li> <div> <strong>Apply the formula:</strong><br> S? = a / (1 - r)<br> S? = 10 / (1 - 0.1)<br> S? = 10 / 0.9<br> S? = 100/9 ? <strong>11.111...</strong> </div> </li> </ul> <p style="background: var(--gray-100); padding: 1rem; border-radius: var(--radius-md); margin-top: 1rem;"> <strong>Answer:</strong> S? = 100/9 or 11? </p> </div> <h3><span class="key-term">Example 2: Negative Common Ratio</span></h3> <div class="worked-example"> <strong>Find: ?(from n=1 to ?) of 3·(-½)??¹</strong> <p>First, let's write out some terms to understand the series:</p> <ul class="step-list"> <li> <div> <strong>Find the first few terms:</strong><br> T? = 3·(-½)? = 3·1 = 3<br> T? = 3·(-½)¹ = -3/2 = -1.5<br> T? = 3·(-½)² = 3/4 = 0.75<br> Series: 3 - 1.5 + 0.75 - 0.375 + ... </div> </li> <li> <div> <strong>Identify a and r:</strong><br> a = 3<br> r = -½ = -0.5 </div> </li> <li> <div> <strong>Check convergence:</strong><br> Is -1 < -0.5 < 1? ? <strong>YES</strong> </div> </li> <li> <div> <strong>Calculate S?:</strong><br> S? = a / (1 - r)<br> S? = 3 / (1 - (-0.5))<br> S? = 3 / (1 + 0.5)<br> S? = 3 / 1.5 = <strong>2</strong> </div> </li> </ul> </div> <h3><span class="key-term">Example 3: Non-Convergent Series</span></h3> <div class="worked-example"> <strong>Does the series 2 + 6 + 18 + 54 + ... have a sum to infinity?</strong> <ul class="step-list"> <li> <div> <strong>Find r:</strong><br> r = 6/2 = 3 </div> </li> <li> <div> <strong>Check convergence:</strong><br> Is -1 < 3 < 1? <strong>NO!</strong> (3 is greater than 1) </div> </li> <li> <div> <strong>Conclusion:</strong><br> The series <strong>DIVERGES</strong>. There is no finite sum to infinity. </div> </li> </ul> </div> <div class="practice-problems"> <h4>? Practice Problems</h4> <p><strong>1.</strong> Find S? for: 8 + 4 + 2 + 1 + ... <em>(Answer: 16)</em></p> <p><strong>2.</strong> Find S? for: 27 - 9 + 3 - 1 + ... <em>(Answer: 81/4 = 20.25)</em></p> <p><strong>3.</strong> Does 5 + 10 + 20 + 40 + ... converge? <em>(Answer: No, r = 2 > 1)</em></p> <p><strong>4.</strong> Find S?: ?(n=1 to ?) of (2/3)? <em>(Answer: 2)</em></p> </div> </div> </section> <!-- Section: Virtual Lab --> <section id="lab"> <div class="section-header" data-toggle="lab-content"> <h2>? Virtual Lab - Infinite Series Explorer</h2> <span class="toggle-icon">?</span> </div> <div class="section-content" id="lab-content"> <p style="margin-bottom: 1.5rem;"> Explore how geometric series behave! Adjust the first term and common ratio to see if the series converges or diverges. Watch the partial sums approach (or not!) the sum to infinity. </p> <div class="lab-zone"> <div class="lab-canvas"> <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; margin-bottom: 1rem;"> <div class="slider-container"> <label>First term (a): <span id="aValue">4</span></label> <input type="range" id="aSlider" min="1" max="20" value="4" step="1"> </div> <div class="slider-container"> <label>Common ratio (r): <span id="rValue">0.5</span></label> <input type="range" id="rSlider" min="-95" max="95" value="50" step="5"> </div> </div> <div id="convergenceIndicator" class="convergence-indicator converges"> ? Series CONVERGES (-1 < r < 1) </div> <div class="series-display" id="seriesDisplay"> <!-- Terms will be added here by JavaScript --> </div> <div class="sum-display"> <div>Partial Sum (S<sub><span id="nTerms">1</span></sub>): <span class="sum-value" id="partialSum">4</span></div> <div style="margin-top: 0.5rem;" id="infinitySumDisplay"> Sum to Infinity (S?): <span class="sum-value" id="infinitySum">8</span> </div> </div> </div> <div class="lab-controls"> <button class="btn-primary" id="addTermBtn">? Add Term</button> <button class="btn-primary" id="animateBtn">?? Animate to 20 terms</button> <button class="btn-secondary" id="resetLabBtn">? Reset</button> </div> <div style="color: var(--gray-300); font-size: 0.9rem; margin-top: 1rem;"> <strong>Experiment ideas:</strong> Try r = 0.5 vs r = 2. What happens when r is negative? When |r| approaches 1? </div> </div> </div> </section> <!-- Section: Real-Time Quiz --> <section id="quiz"> <div class="section-header" data-toggle="quiz-content"> <h2>? Comprehensive Quiz (12 Questions)</h2> <span class="toggle-icon">?</span> </div> <div class="section-content quiz-container" id="quiz-content"> <p>Test your understanding of infinite geometric series. Select an option and get instant feedback.</p> <!-- Question 1 --> <div class="quiz-question" data-question="1"> <h4>Question 1: What condition must r satisfy for a geometric series to converge?</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="false">A) r > 1</div> <div class="quiz-option" data-answer="true">B) -1 < r < 1</div> <div class="quiz-option" data-answer="false">C) r < -1</div> <div class="quiz-option" data-answer="false">D) r = 1</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> A geometric series converges only when the absolute value of r is less than 1, meaning -1 < r < 1 (or |r| < 1). </div> </div> <!-- Question 2 --> <div class="quiz-question" data-question="2"> <h4>Question 2: What is the sum to infinity of: 8 + 4 + 2 + 1 + ...?</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="false">A) 8</div> <div class="quiz-option" data-answer="true">B) 16</div> <div class="quiz-option" data-answer="false">C) 15</div> <div class="quiz-option" data-answer="false">D) The series diverges</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> a = 8, r = 0.5. Since |0.5| < 1, it converges. S? = 8/(1-0.5) = 8/0.5 = 16. </div> </div> <!-- Question 3 --> <div class="quiz-question" data-question="3"> <h4>Question 3: Does the series 1 + 3 + 9 + 27 + ... have a sum to infinity?</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="false">A) Yes, S? = 40</div> <div class="quiz-option" data-answer="false">B) Yes, S? = -0.5</div> <div class="quiz-option" data-answer="true">C) No, the series diverges</div> <div class="quiz-option" data-answer="false">D) Yes, S? = ?</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> r = 3, which is greater than 1. Since |r| > 1, the series diverges and has no finite sum. </div> </div> <!-- Question 4 --> <div class="quiz-question" data-question="4"> <h4>Question 4: What is the formula for sum to infinity?</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="false">A) S? = a·r/(1-r)</div> <div class="quiz-option" data-answer="true">B) S? = a/(1-r)</div> <div class="quiz-option" data-answer="false">C) S? = a/(r-1)</div> <div class="quiz-option" data-answer="false">D) S? = a(1-r)</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> The correct formula is S? = a/(1-r), valid only when -1 < r < 1. </div> </div> <!-- Question 5 --> <div class="quiz-question" data-question="5"> <h4>Question 5: Find S? for the series: 12 - 6 + 3 - 1.5 + ...</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="true">A) 8</div> <div class="quiz-option" data-answer="false">B) 6</div> <div class="quiz-option" data-answer="false">C) 24</div> <div class="quiz-option" data-answer="false">D) 4</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> a = 12, r = -6/12 = -0.5. S? = 12/(1-(-0.5)) = 12/1.5 = 8. </div> </div> <!-- Question 6 --> <div class="quiz-question" data-question="6"> <h4>Question 6: What happens to r? as n ? ? when |r| < 1?</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="true">A) r? ? 0</div> <div class="quiz-option" data-answer="false">B) r? ? 1</div> <div class="quiz-option" data-answer="false">C) r? ? ?</div> <div class="quiz-option" data-answer="false">D) r? ? r</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> When |r| < 1, repeatedly multiplying by r makes the result smaller and smaller, approaching 0. </div> </div> <!-- Question 7 --> <div class="quiz-question" data-question="7"> <h4>Question 7: For which value of r does the series 5 + 5r + 5r² + ... converge?</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="false">A) r = 2</div> <div class="quiz-option" data-answer="false">B) r = -1</div> <div class="quiz-option" data-answer="true">C) r = 0.8</div> <div class="quiz-option" data-answer="false">D) r = 1</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> Only r = 0.8 satisfies -1 < r < 1. The others are outside this range. </div> </div> <!-- Question 8 --> <div class="quiz-question" data-question="8"> <h4>Question 8: If S? = 20 and a = 4, find r.</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="false">A) r = 0.2</div> <div class="quiz-option" data-answer="true">B) r = 0.8</div> <div class="quiz-option" data-answer="false">C) r = 5</div> <div class="quiz-option" data-answer="false">D) r = -0.8</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> 20 = 4/(1-r) ? 1-r = 4/20 = 0.2 ? r = 1 - 0.2 = 0.8. </div> </div> <!-- Question 9 --> <div class="quiz-question" data-question="9"> <h4>Question 9: Which statement about the series 2 - 2 + 2 - 2 + ... is TRUE?</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="false">A) It converges to 2</div> <div class="quiz-option" data-answer="false">B) It converges to 0</div> <div class="quiz-option" data-answer="false">C) It converges to 1</div> <div class="quiz-option" data-answer="true">D) It diverges</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> r = -1, which does not satisfy -1 < r < 1. The sum alternates between 2 and 0, never settling on a single value. </div> </div> <!-- Question 10 --> <div class="quiz-question" data-question="10"> <h4>Question 10: Find the sum: ?(n=1 to ?) of 5·(?)?</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="false">A) 15</div> <div class="quiz-option" data-answer="false">B) 7.5</div> <div class="quiz-option" data-answer="true">C) 2.5</div> <div class="quiz-option" data-answer="false">D) 5</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> T? = 5·(?)¹ = 5/3. So a = 5/3, r = ?. S? = (5/3)/(1-?) = (5/3)/(?) = 5/2 = 2.5. </div> </div> <!-- Question 11 --> <div class="quiz-question" data-question="11"> <h4>Question 11: Can an arithmetic series ever have a finite sum to infinity?</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="true">A) No, arithmetic series always diverge (when d ? 0)</div> <div class="quiz-option" data-answer="false">B) Yes, when d < 1</div> <div class="quiz-option" data-answer="false">C) Yes, when d is negative</div> <div class="quiz-option" data-answer="false">D) Yes, when a < 0</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> Arithmetic series (with constant difference d ? 0) always diverge because we keep adding the same amount forever. Only geometric series with |r| < 1 can converge. </div> </div> <!-- Question 12 --> <div class="quiz-question" data-question="12"> <h4>Question 12: If the first term is 100 and S? = 200, what is r?</h4> <div class="quiz-options"> <div class="quiz-option" data-answer="true">A) r = 0.5</div> <div class="quiz-option" data-answer="false">B) r = 2</div> <div class="quiz-option" data-answer="false">C) r = -0.5</div> <div class="quiz-option" data-answer="false">D) r = 0.25</div> </div> <div class="quiz-feedback"> <strong>Explanation:</strong> 200 = 100/(1-r) ? 1-r = 100/200 = 0.5 ? r = 0.5. </div> </div> <div class="quiz-reset"> <button class="btn-secondary" id="resetQuizBtn">? Retry Quiz</button> </div> </div> </section> <!-- Section: Video Tutorial --> <section id="video"> <div class="section-header" data-toggle="video-content"> <h2>? Video Tutorial</h2> <span class="toggle-icon">?</span> </div> <div class="section-content" id="video-content"> <p style="color: var(--gray-600); margin-bottom: 1rem;"> Watch this comprehensive tutorial on infinite geometric series and the sum to infinity formula! </p> <div class="video-container"> <div class="video-placeholder" id="videoPlaceholder"> <div class="play-btn" id="playVideoBtn"> <svg width="28" height="28" viewBox="0 0 24 24" fill="var(--navy)"> <path d="M8 5v14l11-7z"/> </svg> </div> <p>Click to play: Infinite Geometric Series & Convergence</p> </div> </div> <p style="font-size: 0.9rem; color: var(--gray-500); margin-top: 1rem;"> Duration: 20 minutes | Covers: Convergence conditions, Sum to infinity formula, Worked examples </p> </div> </section> <!-- Section: Live Tutor CTA --> <section id="tutor"> <div class="section-header" data-toggle="tutor-content"> <h2>??? Live Tutoring Support</h2> <span class="toggle-icon">?</span> </div> <div class="section-content" id="tutor-content"> <div class="tutor-cta"> <h3>Need Help with Infinite Series?</h3> <p style="margin-bottom: 1.5rem;"> Our expert mathematics tutors can help you understand convergence, divergence, and the sum to infinity formula. Get personalized guidance on when and how to apply these concepts! </p> <button class="btn-primary" id="openTutorModal">? Book a Session Now</button> <p style="margin-top: 1rem; font-size: 0.95rem;"> Quick questions? WhatsApp us at <a href="tel:0670957879">067 095 7879</a> </p> </div> </div> </section> </main> <!-- ===== FLOATING TUTOR BUTTON ===== --> <div class="floating-tutor" id="floatingTutor" title="Chat with a tutor">?</div> <!-- ===== MODAL ===== --> <div id="tutorModal" class="modal"> <div class="modal-content"> <div class="modal-header"> <h2>Live Tutoring Session</h2> <button class="close-btn" id="closeModal">&times;</button> </div> <p style="margin-bottom: 1rem; color: var(--gray-600);"> Book a session with an expert Grade 12 Mathematics tutor specializing in infinite series and convergence. </p> <form id="tutorForm" style="display: flex; flex-direction: column; gap: 1rem;"> <input type="text" placeholder="Your Name" required> <input type="email" placeholder="Your Email" required> <textarea placeholder="What specific topics do you need help with? (Convergence, Sum to infinity, etc.)" rows="4"></textarea> <select style="padding: 0.75rem; border: 2px solid var(--gray-200); border-radius: var(--radius-md);"> <option>Select session type</option> <option>Quick 30-minute session</option> <option>1-hour comprehensive session</option> <option>Exam preparation</option> <option>Homework help</option> </select> <button type="submit" class="btn-primary" style="width: 100%; margin-top: 0.5rem;">Book Session</button> </form> </div> </div> <!-- ===== FOOTER NAVIGATION ===== --> <footer class="lesson-nav"> <a href="contents.html" class="nav-btn">? Subject Contents</a> <div class="nav-center"> <a href="less1-5.html" class="nav-btn"><span>?</span> Previous Lesson</a> </div> <a href="less1-7.html" class="nav-btn primary">Next Lesson (1.7) ?</a> </footer> <!-- ===== JAVASCRIPT ===== --> <script> // ========================================== // COLLAPSIBLE SECTIONS // Toggles section visibility when header is clicked // Handles both expanding and collapsing states // ========================================== document.querySelectorAll('.section-header').forEach(header => { header.addEventListener('click', function() { const targetId = this.getAttribute('data-toggle'); const content = document.getElementById(targetId); // Toggle collapsed class on both header and content this.classList.toggle('collapsed'); content.classList.toggle('collapsed'); }); }); // ========================================== // SMOOTH SCROLL & ACTIVE NAV HIGHLIGHTING // Updates active nav link based on scroll position // ========================================== const sections = document.querySelectorAll('main section'); const navLinks = document.querySelectorAll('.nav-link'); window.addEventListener('scroll', () => { let current = ''; sections.forEach(section => { const sectionTop = section.offsetTop; if (scrollY >= sectionTop - 160) { current = section.getAttribute('id'); } }); navLinks.forEach(link => { link.classList.remove('active'); if (link.getAttribute('href') === '#' + current) { link.classList.add('active'); } }); }); // ========================================== // VIRTUAL LAB - INFINITE SERIES EXPLORER // Interactive visualization of convergent/divergent series // ========================================== // Lab state variables let labState = { a: 4, r: 0.5, terms: [], currentN: 0, animationId: null }; // Get DOM elements const aSlider = document.getElementById('aSlider'); const rSlider = document.getElementById('rSlider'); const aValueDisplay = document.getElementById('aValue'); const rValueDisplay = document.getElementById('rValue'); const seriesDisplay = document.getElementById('seriesDisplay'); const convergenceIndicator = document.getElementById('convergenceIndicator'); const partialSumDisplay = document.getElementById('partialSum'); const infinitySumDisplay = document.getElementById('infinitySum'); const infinitySumContainer = document.getElementById('infinitySumDisplay'); const nTermsDisplay = document.getElementById('nTerms'); // Initialize the lab function initLab() { updateLabFromSliders(); resetLab(); addTerm(); } // Update lab state from slider values function updateLabFromSliders() { labState.a = parseInt(aSlider.value); labState.r = parseInt(rSlider.value) / 100; aValueDisplay.textContent = labState.a; rValueDisplay.textContent = labState.r.toFixed(2); updateConvergenceIndicator(); } // Update the convergence indicator based on r value function updateConvergenceIndicator() { const converges = Math.abs(labState.r) < 1; if (converges) { convergenceIndicator.className = 'convergence-indicator converges'; convergenceIndicator.innerHTML = '? Series CONVERGES (-1 < r < 1)'; infinitySumContainer.style.display = 'block'; // Calculate and display S? const sInfinity = labState.a / (1 - labState.r); infinitySumDisplay.textContent = sInfinity.toFixed(4); } else { convergenceIndicator.className = 'convergence-indicator diverges'; convergenceIndicator.innerHTML = '? Series DIVERGES (|r| ? 1)'; infinitySumContainer.style.display = 'none'; } } // Calculate partial sum of the series function calculatePartialSum() { if (labState.terms.length === 0) return 0; return labState.terms.reduce((sum, term) => sum + term, 0); } // Add a new term to the series function addTerm() { const n = labState.currentN; const term = labState.a * Math.pow(labState.r, n); labState.terms.push(term); labState.currentN++; renderSeries(); updateSumDisplay(); } // Render the series terms in the display function renderSeries() { seriesDisplay.innerHTML = ''; const maxTermsToShow = 8; const termsToShow = labState.terms.slice(-maxTermsToShow); const startIndex = Math.max(0, labState.terms.length - maxTermsToShow); if (startIndex > 0) { const ellipsis = document.createElement('span'); ellipsis.className = 'series-term plus visible'; ellipsis.textContent = '...'; seriesDisplay.appendChild(ellipsis); } termsToShow.forEach((term, i) => { if (i > 0 || startIndex > 0) { const plus = document.createElement('span'); plus.className = 'series-term plus visible'; plus.textContent = term >= 0 ? '+' : ''; seriesDisplay.appendChild(plus); } const termEl = document.createElement('span'); termEl.className = 'series-term'; termEl.textContent = formatTerm(term); seriesDisplay.appendChild(termEl); // Animate the term appearing setTimeout(() => { termEl.classList.add('visible'); }, 50); }); // Add ellipsis at the end const ellipsisEnd = document.createElement('span'); ellipsisEnd.className = 'series-term plus visible'; ellipsisEnd.textContent = '+ ...'; seriesDisplay.appendChild(ellipsisEnd); } // Format term for display function formatTerm(term) { if (Math.abs(term) < 0.0001 && term !== 0) { return term.toExponential(2); } return term.toFixed(4).replace(/\.?0+$/, ''); } // Update the sum display function updateSumDisplay() { const partialSum = calculatePartialSum(); partialSumDisplay.textContent = partialSum.toFixed(6); nTermsDisplay.textContent = labState.currentN; } // Reset the lab function resetLab() { // Stop any running animation if (labState.animationId) { clearInterval(labState.animationId); labState.animationId = null; } labState.terms = []; labState.currentN = 0; seriesDisplay.innerHTML = ''; updateSumDisplay(); } // Animate adding terms up to 20 function animateToTwenty() { resetLab(); let count = 0; labState.animationId = setInterval(() => { if (count >= 20) { clearInterval(labState.animationId); labState.animationId = null; return; } addTerm(); count++; }, 200); } // Event listeners for sliders aSlider.addEventListener('input', () => { updateLabFromSliders(); resetLab(); addTerm(); }); rSlider.addEventListener('input', () => { updateLabFromSliders(); resetLab(); addTerm(); }); // Event listeners for buttons document.getElementById('addTermBtn').addEventListener('click', addTerm); document.getElementById('animateBtn').addEventListener('click', animateToTwenty); document.getElementById('resetLabBtn').addEventListener('click', () => { resetLab(); addTerm(); }); // Initialize lab on page load initLab(); // ========================================== // QUIZ FUNCTIONALITY // Handles answer selection and feedback display // ========================================== document.querySelectorAll('.quiz-option').forEach(option => { option.addEventListener('click', function() { const question = this.closest('.quiz-question'); if (question.classList.contains('answered')) return; question.classList.add('answered'); const options = question.querySelectorAll('.quiz-option'); const feedback = question.querySelector('.quiz-feedback'); const isCorrect = this.dataset.answer === 'true'; options.forEach(opt => opt.classList.add('disabled')); if (isCorrect) { this.classList.add('correct'); } else { this.classList.add('incorrect'); options.forEach(opt => { if (opt.dataset.answer === 'true') { opt.classList.add('correct'); } }); } feedback.classList.add('show'); }); }); // Reset Quiz - clears all answers and feedback document.getElementById('resetQuizBtn').addEventListener('click', () => { document.querySelectorAll('.quiz-question').forEach(question => { question.classList.remove('answered'); question.classList.remove('scored'); question.querySelectorAll('.quiz-option').forEach(opt => { opt.classList.remove('correct', 'incorrect', 'disabled'); }); question.querySelector('.quiz-feedback').classList.remove('show'); }); quizScore = 0; const floatingTutor = document.getElementById('floatingTutor'); floatingTutor.textContent = '?'; floatingTutor.title = 'Chat with a tutor'; }); // ========================================== // VIDEO PLAYER // Loads YouTube video when play button clicked // ========================================== document.getElementById('playVideoBtn').addEventListener('click', () => { const placeholder = document.getElementById('videoPlaceholder'); placeholder.innerHTML = ` <iframe src="https://www.youtube.com/embed/Gs1f9Cx7xGg?autoplay=1" title="Infinite Geometric Series" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen> </iframe> `; }); // ========================================== // MODAL FUNCTIONALITY // Controls tutor booking modal open/close // ========================================== const modal = document.getElementById('tutorModal'); const openModalBtns = [ document.getElementById('openTutorModal'), document.getElementById('floatingTutor') ]; const closeModalBtn = document.getElementById('closeModal'); openModalBtns.forEach(btn => { btn.addEventListener('click', () => { modal.classList.add('active'); document.body.style.overflow = 'hidden'; }); }); closeModalBtn.addEventListener('click', closeModal); modal.addEventListener('click', (e) => { if (e.target === modal) closeModal(); }); document.addEventListener('keydown', (e) => { if (e.key === 'Escape') closeModal(); }); function closeModal() { modal.classList.remove('active'); document.body.style.overflow = ''; } // Handle form submission document.getElementById('tutorForm').addEventListener('submit', (e) => { e.preventDefault(); alert('Thank you! Your session has been booked. We will send confirmation details to your email.'); closeModal(); e.target.reset(); }); // ========================================== // QUIZ SCORE TRACKER // Tracks correct answers and shows final score // ========================================== let quizScore = 0; const totalQuestions = 12; document.addEventListener('click', (e) => { if (e.target.classList.contains('quiz-option')) { const question = e.target.closest('.quiz-question'); if (!question.classList.contains('scored')) { question.classList.add('scored'); if (e.target.dataset.answer === 'true') { quizScore++; } // Update floating tutor with score const floatingTutor = document.getElementById('floatingTutor'); floatingTutor.textContent = `? ${quizScore}/${totalQuestions}`; floatingTutor.title = `Quiz Score: ${quizScore}/${totalQuestions}`; // Show completion message if all questions answered if (document.querySelectorAll('.quiz-question.scored').length === totalQuestions) { setTimeout(() => { const percentage = Math.round((quizScore / totalQuestions) * 100); let message = `Quiz Complete! You scored ${quizScore}/${totalQuestions} (${percentage}%)`; if (percentage >= 80) message += " ? Excellent understanding of infinite series!"; else if (percentage >= 60) message += " ? Good work! Review the convergence conditions."; else message += " ? Review the lesson material and try again!"; alert(message); }, 500); } } } }); // ========================================== // CHEAT SHEET PRINT BUTTON // Generates printable formula summary // ========================================== const printBtn = document.createElement('button'); printBtn.className = 'btn-secondary'; printBtn.style.position = 'fixed'; printBtn.style.bottom = '90px'; printBtn.style.left = '1.5rem'; printBtn.style.zIndex = '998'; printBtn.innerHTML = '? Cheat Sheet'; printBtn.title = 'Print infinite series cheat sheet'; document.body.appendChild(printBtn); printBtn.addEventListener('click', () => { const printContent = ` <html> <head> <title>Infinite Geometric Series Cheat Sheet</title> <style> body { font-family: Arial, sans-serif; padding: 20px; max-width: 800px; margin: 0 auto; } h1 { color: #1a2744; border-bottom: 2px solid #00d4aa; padding-bottom: 10px; } h2 { color: #1a2744; margin-top: 25px; } .section { margin: 20px 0; } .formula { background: #1a2744; color: white; padding: 20px; margin: 15px 0; border-radius: 8px; text-align: center; font-family: monospace; font-size: 1.3em; } .condition { background: #d1fae5; color: #065f46; padding: 15px; border-left: 4px solid #10b981; margin: 15px 0; } .warning { background: #fee2e2; padding: 15px; border-left: 4px solid #ef4444; margin: 15px 0; } table { border-collapse: collapse; width: 100%; margin: 15px 0; } th, td { border: 1px solid #ddd; padding: 10px; text-align: left; } th { background: #1a2744; color: white; } .converge { color: #10b981; font-weight: bold; } .diverge { color: #ef4444; font-weight: bold; } </style> </head> <body> <h1>Infinite Geometric Series Cheat Sheet</h1> <div class="section"> <h2>The Big Formula</h2> <div class="formula"> S? = a / (1 - r) </div> <div class="condition"> <strong>CONVERGENCE CONDITION:</strong> This formula ONLY works when -1 < r < 1 (i.e., |r| < 1) </div> </div> <div class="section"> <h2>Convergence vs Divergence</h2> <table> <tr><th>If r...</th><th>Series...</th><th>Sum to Infinity</th></tr> <tr><td>|r| < 1</td><td class="converge">CONVERGES</td><td>Use S? = a/(1-r)</td></tr> <tr><td>|r| ? 1</td><td class="diverge">DIVERGES</td><td>No finite sum exists</td></tr> <tr><td>r = 1</td><td class="diverge">DIVERGES</td><td>Sum grows forever</td></tr> <tr><td>r = -1</td><td class="diverge">DIVERGES</td><td>Sum oscillates</td></tr> </table> </div> <div class="section"> <h2>Why It Works</h2> <p>When |r| < 1, the term r? ? 0 as n ? ?</p> <p>So in S? = a(1-r?)/(1-r), the r? disappears, leaving S? = a/(1-r)</p> </div> <div class="warning"> <strong>?? REMEMBER:</strong> Arithmetic series NEVER converge (when d ? 0). Only geometric series with |r| < 1 can have a finite sum to infinity. </div> <div class="section"> <h2>Quick Examples</h2> <p><strong>8 + 4 + 2 + 1 + ...</strong> ? r = 0.5 (converges!) ? S? = 8/(1-0.5) = 16</p> <p><strong>3 - 1.5 + 0.75 - ...</strong> ? r = -0.5 (converges!) ? S? = 3/(1.5) = 2</p> <p><strong>2 + 6 + 18 + ...</strong> ? r = 3 (diverges!) ? No S?</p> </div> <p style="margin-top: 30px; font-style: italic; color: #666;"> PassProof Academy - Grade 12 Mathematics </p> </body> </html> `; const printWindow = window.open('', '_blank'); printWindow.document.write(printContent); printWindow.document.close(); printWindow.print(); }); </script> </body> </html>". Create a new single page HTML/CSS/JavaScript lesson page ensuring collapsible section-content and lab/simulation script is functional, with this lesson text " “
